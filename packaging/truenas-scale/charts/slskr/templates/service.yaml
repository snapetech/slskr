apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-slskr
spec:
  type: {{ .Values.service.type }}
  selector:
    app: slskr
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
