# slskd, slskdN, and slskNet.Runtime Parity Ledger

Date: 2026-07-17

This ledger tracks the work required for `slskR` to reach full feature,
function, and security parity with the frozen upstream targets:

The ordered path to closure and the strict completion contract are maintained in
[`completion-plan.md`](completion-plan.md). This ledger supplies detailed evidence;
the completion plan controls prioritization and the final zero-gap decision.

- slskd `16e5d86ec9a91120f3ef40b85cb22036566b788a`
- slskdN `65a14a8b821de4df4ab7ef3ab3b156d7206837a3`
- slskNet.Runtime `af73ff3f84fda7ba890bb5aea3adf712e5400cf6`

`slskR` remains a Rust-native implementation. Parity means the externally
visible behavior, protocol semantics, security posture, API shapes, UI
workflows, and operator guarantees match unless an entry is explicitly marked
as a compatibility acknowledgement or intentionally Rust-native.

## Baseline

- `slskR` scaffold baseline: `f01cdc30`, 2026-04-30.
- `slskR` WebUI port baseline: `6b0809cd`, 2026-05-04.
- Current endpoint shape result: `scripts/check-endpoint-parity-drift.sh`
  inventories 64 slskd calls and 410 slskdN calls and reports all 417 calls in
  their union present in the slskR WebUI.
- The active literal-parity audit freezes slskd at
  `16e5d86ec9a91120f3ef40b85cb22036566b788a`, slskdN at
  `65a14a8b821de4df4ab7ef3ab3b156d7206837a3`, and slskNet.Runtime at
  `af73ff3f84fda7ba890bb5aea3adf712e5400cf6`. Newer upstream changes start a
  new delta; they do not silently move this audit's evidence boundary.
- The post-freeze delta is audited through slskdN
  `db9d6eee2af1b484c62495d4fb3683f7009a15f4` (192 non-merge commits, 87
  touching `src` or `config`) and the unchanged slskNet.Runtime head above.
- `slskNet.Runtime` comparison baseline: fork changes since Soulseek.NET
  `10.0.0`, plus local commits after the `slskR` scaffold date.
- `slskdN` comparison baseline: local commits after the `slskR` scaffold date.

Endpoint coverage is not sufficient for closure. A route is complete only when
its response shape, mutation semantics, auth/CSRF/rate-limit posture, UI usage,
and regression coverage match the parity target.

## Current Status

Overall closure: **NOT ACHIEVED**. Route presence, compatibility projections,
and selected live paths are evidence inputs, not proof of literal 1:1 parity.
The remaining exhaustive API, configuration, persistence/restart, UI,
operator, failure-path, and bidirectional interoperability matrices must reach
zero unproved or divergent behavior for both targets.

`scripts/audit-parity-manifest.py --check-frozen` now combines the frozen
configuration, controller-route, WebUI-workflow, protocol, persistence/lifecycle,
security, operator/packaging, and live-interoperability inventories into 19,122 unique
proof cases. Its current certification-ledger classification is 662 complete, 0 partial, 191
missing, and 18,269 needing behavioral proof, with zero missing denominators.
The proof cases intentionally have different granularity; the manifest reports
their raw 3.46% closure ratio without treating it as weighted feature value.

The executable configuration ledger currently reports **245 complete, 0
partial, and 191 missing** leaves out of the 436-path frozen union.
All 15 Lidarr leaves and all 7 Spotify leaves are now complete. Lidarr has real
paged wanted sync, scheduling, wishlist creation, completed-directory imports,
path mapping, and manual-import proof. Spotify has real PKCE connection,
encrypted token persistence, refresh/disconnect lifecycle, client-credentials
fallback, provider URL/URI parsing, paged imports, market propagation, request
timeouts, and frozen configuration/controller proof.
`web.max_request_body_size` is complete with its frozen 10 MiB default, YAML-only
exposure, typed and range validation, current/startup projection, watched
restart signaling, retained pre-restart enforcement, post-restart activation,
and exact boundary proof against slskdN. Frozen slskdN reports an oversized body
as generic 500 Problem Details; slskR preserves that profile behavior, while
draft fix `snapetech/slskdN#276` corrects upstream to 413. The seven
`web.rate_limiting.*` leaves are complete with target-specific defaults,
YAML-only exposure, typed and structural validation, startup/current
projection, independent anonymous API, federation inbox, mesh, event-injection,
and warm-cache fixed-window policies, zero-window coercion, authenticated
bypass ordering, disabled negative values, frozen non-positive-permit 500
behavior, watched restart signaling, retained pre-restart limits, post-restart
activation, and full frozen differential proof. The five `web.cors.*` leaves
are complete with disabled, explicit allowlist, wildcard, credentialed,
preflight, ordinary-response, exposed-header, typed-array validation, and
startup/current projection behavior proven against frozen slskdN. Watched CORS
values are projected but continue using the startup policy without setting
`pendingRestart`; this surprising no-cue behavior is the frozen contract, and a
manual restart activates the watched policy in both implementations. The
credential-plus-wildcard startup rejection is also proven with
`web.enforce_security=true`. The slskdN-only `web.allow_remote_no_auth` and
`web.authentication.passthrough.allowed_cidrs` leaves are complete with YAML,
environment, and CLI layering where exposed; loopback-only defaults; exact and
mismatched CIDR decisions; invalid-CIDR ignoring; anonymous-route bypass;
watched projection; startup-bound activation; target restart signaling; typed
validation; and both enforced startup rejection rules proven against the frozen
daemon. `web.authentication.disabled` is complete across both frozen targets:
defaults, passthrough, YAML/environment/CLI precedence, session bootstrap,
credential login, target-specific JWT lifetime, protected-route authorization,
watched projection, pending restart, and post-restart enforcement are
differential-proven. The adjacent username, password, JWT key, and JWT TTL
leaves are also complete through defaults and nulls, YAML/environment/long-and-
short-CLI precedence, exact validation, live credential replacement, target-
specific secret debug projection, JWT restart-only activation, token lifetime,
and cross-restart signature proof. `web.enforce_security` is complete with false
default, YAML/environment/`--enforce-security` precedence, typed validation,
current/startup projection, watched `pendingRestart`, restart persistence,
warning-versus-rejection behavior, all CORS/no-auth/CIDR/memory-dump rules,
frozen weak-metrics prevalidation, and unconditional unsupported-audio-hash
startup rejection. Watched enforcement and the hidden
`flags.hash_from_audio_file_enabled` value project without applying startup
hardening until restart, matching the frozen lifecycle. The slskdN-only
`diagnostics.allow_memory_dump` and `diagnostics.allow_remote_dump` leaves are
complete with false defaults, YAML/environment precedence, typed and structural
validation, current/startup projection, restart signaling, disabled `404`,
remote `403`, successful binary stream and exact download headers, loopback and
remote policy, restart persistence, and `MemoryDumpWithAuthDisabled` enforced
startup rejection proven against the frozen daemon. The frozen controller reads
watched diagnostics values immediately even while setting `pendingRestart`; the
slskR compatibility profile reproduces that split lifecycle. The
case-sensitive-regex differential now proves startup/watch/restart lifecycle,
filtered share counts, slskdN filesystem-library queries, and live incoming
Soulseek search responses against both targets. The protocol proof preserves
slskd's endpoint-before-blacklist ordering and slskdN's username-first
short-circuit, and verifies that case-distinct endpoint cache keys do not
alias. The controller regex engine and differential corpus now cover positive
and negative lookaround, numbered and named backreferences, atomic groups,
capture and assertion conditionals, global/scoped explicit-capture mode,
mixed named/unnamed .NET capture numbering, control-character escapes,
default/multiline `$`, `\Z`, invariant Unicode case folds (including dotted
and dotless I, Kelvin sign, long S, sharp S, and final sigma), duplicate and
explicitly numbered named-capture slots, .NET Unicode category/property
validation, and case-mode behavior on the share,
incoming-search, and username-blacklist runtime paths. Validation also rejects
possessive quantifiers, recursion, backtracking verbs, `\K`, braced hex, and
other foreign-dialect constructs rejected by frozen .NET. slskdN runtime compatibility for documented
`transfers.groups` and top-level `groups` placement is also proven, including
top-level precedence. Frozen slskdN's YAML validation endpoint does not apply
that same placement compatibility; upstream draft PR #273 fixes the defect.
Those regex leaves remain partial because exact behavior for every .NET
`Regex` construct, culture rule, and timeout/failure path is not yet proven.
Both frozen daemons also drop the configured case-insensitive mode when
recompiling changed search-request filters. Draft slskdN PR #274 fixes that
upstream defect; the matching slskd defect is reported here without a PR.
The web rate-limit differential also found that frozen slskdN's JWT-only
default authentication stage leaves valid API-key callers anonymous when the
earlier global limiter selects a partition, so those callers can exhaust the
anonymous API bucket despite the explicit authenticated bypass. Draft slskdN
PR #275 selects API-key authentication before partitioning and adds focused
unit, HTTP-host, and live differential regression proof.

The frozen controller inventories also contain a real target conflict: 31 of
the 88 shared slskd/slskdN routes declare different authorization roles.
slskd generally accepts any authenticated credential on those routes while
slskdN requires read/write or administrator access. One fixed policy cannot be
literally identical to both. `SLSKR_CONTROLLER_COMPATIBILITY_TARGET` now
selects exact generated `slskd` or `slskdn` authorization registries. An
exhaustive test applies anonymous, access-level, scheme, and scope checks to all
91 + 678 rules. That test exposed and fixed literal-route precedence over
parameter routes (`profile/me` versus `profile/{peerId}`). Live HTTP auth
profiling now proves read-only access to slskd `/logs`, administrator-only
slskdN `/logs`, and authenticated slskdN `/profile/me`. Every non-auth target
conflict remains open.

`scripts/check-controller-options-differential.sh` now builds and launches both
frozen daemons plus both slskR controller profiles. It compares the complete
`GET /api/v0/options` JSON values after normalizing only instance-specific
download directories and explicitly selected HTTP/HTTPS ports. It also compares
the exact status, content type, and response bytes for null, array, and valid
`PATCH /api/v0/options` requests plus valid and invalid
`POST /api/v0/options/yaml/validate` requests. Both target profiles pass.
Watcher, reconnect, restart, exhaustive validation, and non-default leaf
matrices remain open. The same gate now also launches both profiles with exact
custom downloads/incomplete environment and CLI paths, compares the resulting
options DTOs, and compares direct, recursive, and base64-subdirectory
file-management DTOs after normalizing only filesystem timestamps. It also
compares enabled deletion of existing files/directories and idempotent missing
files. That extension exposed and removed slskR-only pagination fields, blank
metadata, nested recursive output, uniform child DTOs, and `200` boolean delete
responses. The implementation now matches the frozen flat recursive shape,
target-specific child collections, real attributes/timestamps, default deny,
and enabled `204` behavior. The share fixture now uses an explicit alias plus
an excluded subtree. Both profiles compare the configured options, complete
share list, all-share contents, ID-addressed resources, per-share contents,
excluded-share resources, empty excluded contents, alias/lowercase-ID negative
lookups, and the same projections after a daemon stop/restart. The comparison
is byte/status/content-type exact after JSON canonicalization: uppercase SHA-1
IDs are case-sensitive, virtual directory names use Soulseek backslashes,
excluded files never enter the index, and local/raw paths and statistics remain
stable after persistence rehydration.
The same gate now mutates `slskd.yml` while each daemon is running. It proves
the malformed-YAML fallback, immediate option/share-definition replacement,
omitted pre-scan statistics, retained cache totals, `scanPending` transition,
exact empty `PUT /shares` response, post-scan totals, `no_config_watch` remote
upload restart signal, direct in-place writes under that frozen flag, and a
changed-configuration restart against both targets. `shares.directories` is
therefore admitted as complete by the executable ledger. The same gate proves
the startup, live-watch, `pendingRestart`, immediate file-management behavior,
and changed-configuration restart lifecycle for `directories.downloads` and
`directories.incomplete` against both targets; a hermetic runtime test also
proves transfer allocation follows the watched completed-download root. It
also proves `remote_file_management` changes immediately without requesting a
restart, enforces exact empty-body `403`/`204` delete responses, and preserves
both enabled and disabled states through restart on both targets.
The gate also now covers `remote_configuration` startup/current separation,
live enablement, API self-disable, direct-file re-enable, exact empty-body
forbids, no-restart signaling, enabled/disabled restart persistence, and the
frozen slskdN adversarial-route `500` failure when disabled. It now also
compares every enabled debug-view line, provider annotation, dynamic path and
port, and target-specific secret treatment, and proves slskdN default/custom
adversarial YAML replacement plus invalid-update rollback. The frozen slskdN
settings GET remains a 404 because its controller requests an unregistered
direct options value; the upstream fix is draft PR `snapetech/slskdN#267`.
`remote_configuration` is therefore admitted as complete by the executable
ledger. The adjacent `debug` leaf is complete as well: its separate lifecycle
gate proves frozen YAML-over-environment/CLI precedence, environment/CLI
enablement when YAML omits the property, invalid boolean responses,
startup/current separation, restart signaling in both mutation directions,
startup-fixed endpoint and logger behavior, and application after restart.
`instance_name` is complete. The differential proves the frozen default,
YAML-over-environment and command-line-over-YAML precedence, both startup and
current projections, startup diagnostics, watched restart signaling, retained
startup identity before restart, and restarted identity against both targets.
It also proves null/empty fallback, numeric and boolean string binding,
unvalidated 300-byte/control-character values, and the target-specific JSON
error bodies for array and object values. That matrix removed slskR-only
length/control validation and scalar-type projection differences.
`transfers.download.completed_path_template` is complete for the frozen
slskdN-only surface. Its differential proves YAML-over-environment and
command-line-over-YAML precedence, startup/current/diagnostic projection,
immediate watched application without a restart signal, restart persistence,
and target-exact rejection of array/object values. A focused runtime test
proves the watched template reaches completed-download destination rendering,
not only the controller projection.
`dht.enabled` and `dht.dht_port` are complete for the frozen slskdN-only
surface. The socket-level differential proves enabled and disabled startup,
exact validation, watched current-versus-startup projection, no restart
signal, retention of the startup UDP socket and DHT status before restart,
socket release/rebind after restart, and both disabled-to-enabled and
enabled-to-disabled lifecycles. slskR's `isDhtRunning` projection now follows
bootstrap readiness rather than mere socket creation, matching the target.
`flags.no_connect` is complete as well: a local TCP fixture proves both targets
and slskr suppress the Soulseek server connection at startup, do not mutate the
live session when the watched value changes, set `pendingRestart`, connect only
after restart with the flag cleared, retain the active connection until the
next restart when it is set again, and suppress the connection after that
restart. The differential also proves slskd accepts a loopback listen address,
slskdN rejects it only while connecting, slskdN exposes the watched-invalid
options state as a target-shaped `500`, and valid YAML restores the daemon.
`flags.no_config_watch` is complete: startup-true uploads, direct writes under
the flag, share replacement, restart persistence, and the watched
false→true→restart→false lifecycle all match both targets. The effective
current value controls upload restart signaling, the flag change itself sets
`pendingRestart`, and direct writes deliberately remain live because that is
the frozen behavior.
`soulseek.description` is complete. A login-capable local Soulseek server and
direct peer probe prove the configured value in the actual user-info protocol
response before mutation, immediately after a watched Unicode change, and
after restart on both targets. The slskdN path also proves the exact
now-playing suffix and its removal after clearing the track.
The same differential now completes the `soulseek.listen_ip_address` and
`soulseek.listen_port` paths: exact YAML validation
for malformed/null/numeric/bool IP values and malformed/null/bool/float/text,
negative, boundary, and out-of-range ports; real TCP listener movement; live
Soulseek-server port advertisement; target-specific `pendingReconnect` state;
occupied-port rollback; watched-invalid `500` responses; and
restart persistence. It also preserves a frozen failure: changing only a
wildcard listen IP on the same port fails the Soulseek.NET preflight, so the
options DTO shows the new IP while the old wildcard listener remains live
until restart. The slskdN profile additionally proves the default derived
type-1 listener, its startup/restart socket lifecycle, the frozen live-update
bug that retains the old obfuscated socket and metadata until restart, and the
exact two-at-login/one-per-update `SetListenPort` count and metadata sequence.
Validated fixes for both slskdN listener bugs are open as GitHub draft PR
`snapetech/slskdN#266`; slskR deliberately retains both frozen behaviors for
the pinned snapshot profile.

Both frozen daemons have an additional watched-invalid configuration defect:
after rejecting an invalid listener port through HTTP, a background service can
resolve the invalid options object and nondeterministically stop the host. The
listener differential therefore runs that assertion last and makes no false
in-process recovery claim. The slskd defect is reported here without an
upstream PR. Draft slskdN fix `snapetech/slskdN#270` retains the last valid
options for background services and accepts later valid reloads.

The 38 upload-slot and transfer-group configuration leaves are complete through
target-specific projection and validation, YAML/environment/CLI precedence,
watched and restarted lifecycle, blacklist membership, group controller APIs,
real `QueueUpload` and `PlaceInQueue` handling, priority/FIFO/round-robin
scheduling, and aggregate slot and bandwidth enforcement against both frozen
daemons. This result does not close the broader transfer API, UI, persistence,
or interoperability matrices.

The 14 frozen `transfers.download` leaves are complete through exact dual-target
projection and validation, YAML/environment/CLI precedence, watched and
restarted lifecycle, retry attempts and capped backoff, incomplete-file
resume/overwrite, move-on-success, destination collision policy and chmod,
completed layout, slot admission, aggregate download pacing, and slskdN
auto-replacement enable/threshold/interval behavior. The focused differential
passes both frozen profiles at `/tmp/slskr-options-differential.GRw1tx`.

| Area | Status | Required closure |
| --- | --- | --- |
| Current configuration ledger | Executable inventory; major gap | The current executable audit supersedes older counts embedded later in this table: **227 complete, 0 partial, and 209 missing** out of 436 frozen union leaves. All 22 Lidarr/Spotify configuration leaves are complete with frozen startup layering, live application, validation, persistence/restart, secret projection, and controller proof. Lidarr has paged wanted sync, scheduling, wishlist creation, path mapping, candidate safety, completed-directory imports, and real manual-import HTTP consumers. Spotify has PKCE OAuth, encrypted token persistence, refresh/disconnect lifecycle, app-token fallback, provider target parsing, paging, market selection, timeout enforcement, and source-import proof. YouTube and Last.fm add typed secret configuration, frozen lifecycle/validation proof, paged playlist retrieval, and loved/recent/top-track provider imports. `web.max_request_body_size` is complete through runtime and restart differential proof; frozen slskdN's generic-500 oversized-body defect is preserved by the profile and corrected by draft PR #276. The seven `web.rate_limiting.*` leaves are complete through typed validation, all runtime partitions, frozen failure behavior, watch/restart lifecycle, and differential proof. The five `web.cors.*` leaves are complete through disabled, allowlist, wildcard, credentialed, preflight, response, validation, projection, frozen no-restart-cue, manual-restart, and enforced credential-plus-wildcard startup-rejection differential proof. The slskdN-only `web.allow_remote_no_auth` and `web.authentication.passthrough.allowed_cidrs` leaves are complete through local-default, explicit-CIDR, anonymous-route, validation, watch/restart, and enforced-startup differential proof. `web.authentication.disabled` is complete through dual-target defaults, passthrough, precedence, session login and checks, target-specific JWT lifetimes, watched restart signaling, and post-restart enforcement. Its adjacent username, password, JWT key, and JWT TTL leaves are complete through defaults and nulls, precedence, exact validation, live credential replacement, target-specific secret debug projection, JWT restart-only activation, token lifetime, and cross-restart signature proof. `web.enforce_security` is complete through default, YAML/environment/CLI precedence, validation, projection, watched restart signaling, deferred startup enforcement, warning/rejection behavior, every observable frozen hardening rule, weak-metrics prevalidation, and unsupported-audio-hash restart rejection. The two `diagnostics.*` dump leaves are complete through defaults, YAML/environment precedence, validation, projection, live watched controller behavior with a restart cue, restart persistence, disabled and remote denial, binary streaming, exact download headers, and enforced-startup differential proof. `flags.case_sensitive_reg_ex`, `shares.filters`, `filters.search.request`, and the three `transfers.groups.blacklisted.*` leaves are complete through startup/watch/restart lifecycle, .NET-compatible validation and execution, share scanner and slskdN library fallback, connected Soulseek search filtering, peer blacklist behavior, invariant Unicode casing, and both frozen target differentials. `feature.swagger` is complete with target-specific defaults, YAML/environment/CLI precedence, null/scalar/object validation, current/startup projection, watched `pendingRestart`, unchanged startup route publication before restart, restart persistence, and functional `/swagger`, UI asset, and OpenAPI route proof against both targets. All five `metrics.*` leaves are complete with target-specific credential defaults, secret projection, YAML/environment/CLI precedence, typed and structural validation, startup/current projection, watched restart-only lifecycle, normalized custom route publication, Prometheus content type, no-auth behavior, correct and malformed Basic credentials, slskd's empty unauthorized response, and slskdN's `WWW-Authenticate` response proven against both targets. Exact upstream metric-family/catalogue parity remains a broader telemetry-service gap and is not claimed by the configuration-leaf result. `headless` is complete with default/YAML/environment/CLI precedence, null/scalar/array validation, startup/current projection, watched restart-only lifecycle, UI/static/SPA suppression, retained API access, and empty browser-session-login `403` behavior proven against both targets. `flags.experimental` is complete with false defaults, YAML/environment/`--experimental` precedence, target-specific Boolean validation, current/startup projection, watched restart-only lifecycle, restart persistence, and proof of the frozen no-independent-feature-activation behavior. `flags.no_logo` is complete with false defaults, YAML/environment/`-n|--no-logo` precedence, target-specific Boolean validation, current/startup projection, watched restart-only lifecycle, both frozen target-specific license/banner frames and randomized glyph variants, and suppression after restart. `flags.no_start` is complete with false defaults, YAML/environment/`-x|--no-start` precedence, target-specific Boolean validation, current/startup projection, watched restart-only lifecycle, successful exit with the frozen quit message, and proof that HTTP is never bound after restart. `flags.no_version_check` is complete with false defaults, YAML/environment/CLI precedence, target-specific Boolean validation, current/startup projection, watched restart-only lifecycle, slskd development-build skip behavior, slskdN guarded GitHub release checking, target-specific initial version DTOs, and suppression after restart. `flags.no_share_scan` and `flags.force_share_scan` are complete with default/YAML/environment/CLI precedence, target-specific typed validation, current/startup projection, restart-only watched lifecycle, cache reuse versus forced bypass, skip-without-initialization behavior, and the frozen conflict rule where `no_share_scan` wins when both flags are true. |
| WebUI endpoint shape and workflows | API-call union gate backed; workflows partial | The old 296-line grep list was not a frozen or behavioral inventory. `scripts/audit-upstream-webui-endpoints.mjs` parses non-test API calls from both frozen WebUIs: 64 slskd calls and 410 slskdN calls, a 417-call union. The slskr WebUI now includes all 417 target calls plus three native log-control calls after restoring the exact frozen API modules, the canonical opinions/UserCard workflow, and a download-request client with focused tests. `scripts/check-endpoint-parity-drift.sh` materializes both frozen snapshots and fails on missing or unresolved calls. Call presence does not prove page routing, user actions, response rendering, errors, accessibility, or restart behavior; those workflow differentials remain open. |
| slskd controller API | In progress; route-presence gate backed | The frozen inventory contains 91 routes. The joint controller gate materializes the exact slskd snapshot and currently finds zero generic router fallthroughs, HTML fallbacks, or compatibility fallbacks. Separate inventory found three slskd-only routes; KPI already existed, while real application-dump streaming and batch-download creation were added with focused tests. This does not yet prove DTO, auth, persistence, restart, failure, or live-network equivalence. |
| slskdN controller API | Partial; route-presence gate backed | `scripts/audit-slskdn-controller-routes.mjs` inventories 678 distinct routes. The joint frozen-snapshot gate reaches a dedicated or state-backed handler for all 678 with zero generic router fallthroughs, HTML fallbacks, or compatibility fallbacks. The previous claim of completion was invalid: exclusions, compatibility projections, and successful route probes are not literal behavioral proof. Existing GET/mutation/auth evidence remains useful, but every excluded, generated, timeout, 5xx, persistence, restart, and negative-path case must be resolved or matched before closure. |
| Configuration and startup behavior | Executable inventory; major gap | `scripts/audit-upstream-config-surface.py --check-frozen` derives 215 documented slskd YAML leaves and 416 slskdN leaves (436-path union, 195 common), plus 126/214 environment-variable attributes, 131/217 CLI attributes, 60/74 restart-required properties, and 4/11 reconnect-required properties. The leaf-level ledger currently reports **227 complete, 0 partial, and 209 missing**. `blacklist.enabled`, `blacklist.file`, `debug`, `instance_name`, `shares.directories`, `directories.downloads`, `directories.incomplete`, `dht.enabled`, `dht.dht_port`, `remote_configuration`, `remote_file_management`, `flags.no_connect`, `flags.no_config_watch`, `soulseek.address`, `soulseek.port`, `soulseek.username`, `soulseek.password`, `soulseek.private_message_auto_response.enabled`, `soulseek.private_message_auto_response.message`, `soulseek.private_message_auto_response.cooldown_minutes`, `soulseek.description`, `soulseek.listen_ip_address`, `soulseek.listen_port`, `soulseek.obfuscation.enabled`, `soulseek.obfuscation.listen_port`, `soulseek.obfuscation.mode`, `soulseek.obfuscation.prefer_outbound`, all ten `transfers.download.auto_retry.*` leaves, `transfers.download.completed_path_template`, `web.ip_address`, and `web.port` are complete. Managed blacklist configuration has exact defaults, YAML/environment/CLI precedence, CIDR/P2P/DAT parsing, target-specific P2P behavior, validation, startup/current projection, watched enable/file/disable behavior, restart signaling and persistence, username decision caching, IPv4-mapped address handling, and real blocked browse/folder/search/transfer/user-info protocol behavior proven against both targets. Share configuration has exact frozen YAML/environment/CLI layering, aliases, exclusions, scanner/protocol/API consumers, validation, live file mutation, malformed input, scan lifecycle, no-watch upload/direct-write behavior, persistence, changed-config restart, and response metadata proven against both targets. The two storage roots have startup layering and validation plus dual-target live-watch, `pendingRestart`, immediate file-management behavior, transfer allocation, persistence, and changed-configuration restart proof. Remote file management has exact startup spellings, live false/true authorization changes without a restart signal, both file deletion outcomes, and enabled/disabled restart persistence proven against each target. Remote configuration has exact live authorization, self-disable/direct-file re-enable, restart persistence, full enabled debug-view content, target-specific secret behavior, and slskdN adversarial default/custom/invalid mutation proof. Debug has exact frozen precedence, validation, restart-only current/startup lifecycle, logger posture, endpoint gating, and full configuration-tree output. Instance name has exact default, YAML/environment/CLI precedence, startup/current and diagnostic projection, restart-only watched lifecycle, null/empty fallback, scalar string binding, unvalidated long/control values, and target-specific composite-value validation responses. The completed-path-template leaf has exact slskdN precedence, live watch, startup/current/diagnostic projection, validation, restart persistence, and real destination-renderer proof. The two DHT leaves have exact validation, watched projection, startup-state retention, enabled-status, UDP socket lifecycle, and restart proof; public-bootstrap readiness is type-checked in the differential and behaviorally proven on a controlled local DHT testnet because independent public bootstrap timing is nondeterministic. The three private-message auto-response leaves have exact default, YAML/environment/CLI precedence, typed scalar and boundary validation, startup/current projection, watched enable/payload/cooldown/blank/null/disable behavior, no-restart lifecycle, restart persistence, real inbound challenge classification and outbound Soulseek frame proof, cooldown state, and frozen manual-ack semantics. The ten download auto-retry leaves have exact slskdN defaults and YAML-only exposure, typed scalar, null, object-shape, boundary, and frozen fractional-range validation; startup/current projection; watched enable, disable, and interval/policy application without restart; restart persistence; and a connected Soulseek differential that seeds three eligible failures and proves the configured global and per-peer limits create exactly two retries, contact both selected peers, and remain bounded through the next scheduler cycle under peer cooldown. Local plan/cycle tests cover attempt budgets, alternate-source enablement, search budgets, fractional size tolerance, metadata preservation, and rollback. No-connect has startup layering, real TCP suppression/activation, watched restart-only behavior, both restart directions, target-specific loopback validation, watched-invalid failure projection, and recovery proof against each target. No-config-watch has startup-true upload/direct-write behavior plus watched false/true transitions, current-value upload signaling, restart-only flag application, and both restart directions proven against each target. Soulseek server address and port have exact frozen validation and environment/YAML/CLI precedence, real fixture login, watched current-versus-startup projection, pending-reconnect state, old-connection retention, explicit disconnect/reconnect onto the new endpoint, target-specific disconnected DTO shapes, and restart persistence proven against both targets. Soulseek username and password have exact defaults, secret projection, environment/YAML/CLI precedence, watched current-versus-startup and pending-reconnect behavior, old-session retention, real login-frame username plus password-digest proof after reconnect and restart, API-supplied credential precedence, and target-specific debug rendering proven against both targets. The frozen slskd debug view exposes the raw Soulseek password while slskdN masks it; the compatibility profiles preserve that observable distinction. Soulseek description has controller, live watched protocol response, Unicode framing, restart persistence, and slskdN now-playing suffix/clear proof. The listener leaves have exact startup layering and validation, dual-target watched socket movement/failure, real server advertisement sequencing, occupied-port rollback, target-specific reconnect state, restart persistence, and slskdN derived type-1 listener behavior. Invalid watched listener values produce the frozen `500`; both targets can then terminate nondeterministically when a background service resolves the invalid options object, so no reliable in-process recovery is claimed. The completed HTTP listener leaves have exact target-specific address names and defaults, YAML/environment/CLI precedence, validation responses, IPv4/IPv6 and multi-address socket behavior, watched current-versus-startup projection, restart signaling, retained old socket before restart, socket movement after restart, and occupied-port startup failure proof. Both frozen daemons log the occupied-port failure as fatal but return process status 0; slskR preserves that compatibility behavior. Frozen slskd also returns an uncaught empty-path `ArgumentException` from `/yaml/validate` for `blacklist.file: ""` even when the blacklist is disabled; this is an upstream slskd defect and no slskd PR was opened. The other slskd defects are reported here without PRs, while draft slskdN fixes `snapetech/slskdN#269` and `snapetech/slskdN#270` correct fatal startup exit status and retain the last valid options after rejected reloads. Draft slskdN fix `snapetech/slskdN#271` corrects the frozen integer-operand range annotation that rounds fractional auto-retry tolerances at both boundaries. The four completed obfuscation leaves have exact slskdN-only precedence and validation; explicit and derived type-1 socket/advertisement behavior; frozen watched-versus-restart listener and metadata behavior; and controlled compatibility, disabled, preference-disabled, obfuscated-first, and regular-fallback outbound proof. `soulseek.obfuscation.advertise_regular_port` is complete by reproducing frozen slskdN's validator/runtime split: `/yaml/validate` accepts the incompatible enabled-without-regular-advertisement combination, runtime construction fatals, and the process exits 0. The normal enabled, disabled, watched, restart, socket, metadata, and outbound paths also pass; draft upstream fix `snapetech/slskdN#268` remains separate from frozen-profile compatibility. All eleven frozen FTP configuration leaves are complete with plain, implicit, explicit, and auto FTPS modes; passive binary upload; remote directory creation; overwrite/skip; bounded timeout and retries; target-specific certificate behavior; and post-download runtime hooks. The nine frozen VPN/Gluetun leaves are complete with exact layering, validation, secret projection, target-specific single and multi-forward polling, API-key-over-Basic authentication, readiness projection, Soulseek gating, disconnect-on-loss, and reconnect-on-recovery. The 21 frozen script-integration paths are complete with dynamic dictionaries, all three execution modes, event validation and serialization, target-specific projection and safeguards, and live process differentials against both daemons. All six frozen webhook configuration leaves are complete, including selective self-issued certificate tolerance that retains hostname verification; 209 missing leaves remain open and must be implemented with behavioral proof. Remote configuration is disabled by default; enabled YAML is bounded, atomically persisted with backup, projected with secret redaction, and applied to mapped consumers after restart, while arbitrary uploaded leaves still do not reconfigure their runtime consumers. `--require-complete` deliberately fails until every leaf has YAML, environment, CLI, runtime, lifecycle, validation, precedence, secret, and differential evidence. |
| Core Soulseek protocol codes | Implemented/live core-path proof; distributed proof open | Server, peer, init, and distributed code tables have uniqueness/completeness inventory tests, known-value mappings, unknown-code preservation, bounded-count guards, and round-trip coverage. The 2026-07-15 current-client matrix proves live session, peer-message, search, browse, transfer, and private-message paths against slskdN; an explicit live distributed-tree exchange remains required. |
| Type-1 obfuscation | Implemented/live-proven | Rotated type-1 framing, obfuscated listener demux for `P`, `D`, and `F`, regular-first compatibility mode, explicit obfuscated-first prefer mode, and bidirectional fallback for peer-message, browse, and file-transfer attempts are covered locally. The 2026-07-16 Foundation certification passed all eight checks, including a real public-listener type-1 request/response and indirect peer exchange. |
| Search, browse, transfer projections | Implemented/live-proven | Explicit and autonomous download retry, resume-state preservation, rooted download-path validation, transfer progress/failure projection, stable incremental transfer snapshots and history pages, peer queue-position projection, stuck-download, per-user, speed, download stats, accelerated-download compatibility reports, download batch projection, local peer transfer tests, slskd-style public vs locked/private search response projection, paged search-detail result arrays, paged/filtered search response peer groups, SQLite-backed search result restart hydration, search lifecycle update projection including caller-supplied TTL bounds, search-backed transfer alternative replacement and auto-replacement, and paged/filtered slskd-shaped user browse/directory projections are covered. The 2026-07-15 current-client matrix proves both-direction search, browse, and exact-hash fixture downloads, including slskdN's queued upload-token rotation. |
| Rooms, messages, and social projections | Implemented/live-proven | Room creation failure projection, reconnect-aware room join `503` behavior, client/protocol/API multi-user message sends, sharegroup-backed user group projection, and durable message/room persistence tests are implemented. The current-client matrix proves both-direction private messages; the earlier VPN matrix proves live room join/message/leave behavior. |
| slskdN peer capabilities | Implemented/live-proven | The reserved Soulseek message code, frozen `KSDN` binary envelope, string nonce, overlay port, maximum payload, feature list, optional Ed25519 material, binary canonicalization, and lower-case base32 peer ID match `slskNet.Runtime`. Connection usernames are attached as authenticated observation metadata because they are not signed or serialized by the runtime. The current-client matrix proves a signed slskR hello, exact nonce acknowledgement, Ed25519 verification, and persisted descriptors in both clients' live registries. |
| Mesh rendezvous and overlay | Implemented/live-proven | Exact bounded big-endian JSON framing, `SLSKDNM1` hello/ack with nonce echo, TLS 1.3 self-signed-certificate validation plus explicit SHA-256 pinning, bounded inbound/outbound connections, service deadlines, frozen 30-second read polling/two-minute keepalive/five-minute idle lifecycle, validated ping/pong handling, correlated service calls, and System.Text.Json-compatible byte arrays are implemented. The dispatcher requires a fresh authenticated Soulseek capability and matching peer IP; slskr peers add an Ed25519 hello proof bound to the gateway certificate, while frozen unsigned clients retain the exact capability/IP service path. A bounded Mainline-DHT client announces and looks up the exact SHA-1 `slskdn-mesh-v1` primary and backup keys, exposes live status, and feeds at most four pinned gateway candidates. The current-client matrix proves slskR-to-slskdN TLS 1.3 pin validation, frozen nonce handshake, a correlated `dht.Ping`, and an authenticated Ed25519 `dht.Store` that increased slskdN's stored-key count. Automatic pin rotation remains an optional extension because the frozen capability wire omits the certificate pin. |
| Wishlist interval scheduling | Implemented/live-proven | Client scheduler primitive honors server `WishlistInterval` with positive minimum interval validation; daemon session manager snapshots wishlist terms, records scheduled wishlist searches, and emits `WishlistSearch` while connected. The 2026-07-16 Phase C certification received a positive public-server interval, sent the exact wishlist search message, and retained a healthy connection. |
| VirtualSoulfind v2 intents and catalogue | Implemented/live-proven | The default-enabled v2 API exposes bounded track/release intent queues, stable library-backed artist/release/track catalogue projections, local-library planning, atomic pending-intent claims, execution records, and processor statistics with the sibling's string-enum/camel-case contract. The 2026-07-16 current-client matrix ran create, pending-list, empty-plan, release-intent, process, terminal-status, and exact-once statistic checks against both slskR and slskdN. |
| Pod management services | Implemented/live-proven | Authenticated Pod CRUD, membership/moderation, bindings, and member-only messages use bounded restart-safe atomic JSON stores. `readonly` and `mirror` room bindings join and ingest the live Soulseek room; mirror sends use the normal session command path. `soulseek-dm:` channels project and send the durable private-message conversation. The pinned overlay exposes the frozen `pods` List/Get/Join/Leave/PostMessage/GetMessages service with private-read and member-write authorization. Operator-provided peer-ID/username/endpoint/pin trust also drives frozen-descriptor gateway forwarding. The bounded DHT publisher emits listed Pod metadata and the shared `pod:index:listed` record under the interoperable `pod:metadata:{id}` convention. The 2026-07-16 cross-client matrix created a Pod in slskdN and completed List, Get, Join, PostMessage, GetMessages, and Leave from slskr over the pinned overlay. |
| VPN forwarding services | Implemented/live-proven | Authenticated Pod members can start bounded loopback TCP rules to policy-allowed private TCP destinations through a discovered gateway's pinned TLS 1.3 overlay. The inbound gateway authorizes Pod membership, request bounds/freshness/replay, signed local gateway identity, exact policy, private resolved addresses, per-peer/global quotas, connection ownership, and bounded resolution/dial/write operations. Each direction uses the frozen OpenTunnel/TunnelData/GetTunnelData/CloseTunnel payloads and 8 KiB chunks. Soulseek observations and bounded DHT candidates feed certificate/username-verified connection attempts. The 2026-07-16 matrix created a gateway Pod in slskdN, joined from slskr, opened a private TCP tunnel, round-tripped an exact echo payload, closed the tunnel, and passed the post-transfer soak. Automatic certificate-pin lifecycle/rotation remains an optional wire extension. |
| Security posture | Gate-backed/needs-live-proof | Bind/public-posture checks, WebSocket auth coverage, CSP, OAuth callback state isolation, webhook outbound policy, rate-limit proxy policy, storage/transfer pressure gates, secret scanning, protocol scalar/adversarial gates, and remediation baseline are implemented. Remaining work: required live exposure/interoperability proof in representative deployment environments. |
| Release/packaging/dependency posture | Gate-backed | Release workflow policy, package artifact matrix, release version metadata, dependency hygiene, audit-tooling inventory, SDK gates, OpenAPI drift, docs freshness, and remediation registry checks are implemented. Remaining work: keep gates green as upstream release/CI changes are classified. |

### Frozen distributed-DHT contract

The corrected cross-runtime contract is now explicit and gate-backed:

- String namespaces derive a 160-bit key as `SHA1(UTF8(namespace))`; explicit
  shadow-index keys retain the same 20-byte SHA-1 convention.
- Kademlia requester/node identity is the first 20 bytes of SHA-256 over the
  Ed25519 public key, matching the lower-case base32 peer identity digest.
- Store signatures cover the exact .NET `DhtStore` canonical JSON, including
  `public_key`, `timestamp_ms`, property order, numeric message type, and
  System.Text.Json base64 escaping. Deterministic Rust and .NET vectors verify
  each other.
- Remote admission uses the verified Store signer identity for quotas and
  routing; the overlay's Soulseek username is observation metadata, not a
  self-certifying peer ID.
- Listed Pod discovery uses `pod:metadata:{podId}` plus `pod:index:listed`.

slskdN now routes string-key writes and reads through the distributed service,
performs iterative remote `FindValue`, and lets shadow publishers use that
distributed path. slskR publishes bounded content-peer, reverse peer-content,
shadow-index, Pod metadata, and listed-Pod index records every 30 minutes. The
live matrix proves a pinned authenticated Rust Store is accepted and retained
by slskdN.

## 2026-07-15 Upstream Delta Audit

The previous ledger date is not a current upstream baseline. At the frozen
heads above, the sibling repositories contain 575 non-merge `slskdN` commits
and 42 non-merge `slskNet.Runtime` commits since the explicit start-of-day
cutoff `2026-05-13T00:00:00-06:00`. Of those, 217 and
6 respectively touch `src`. The behavior/security classification against
those snapshots is:

| Upstream change | Classification | Current evidence and remaining closure |
| --- | --- | --- |
| `slskdN` reusable share tokens kept out of URLs (`1100d415d`) | Implemented/hermetic | The React client sends reusable tokens in `X-Share-Token`, the daemon accepts the dedicated header or `Authorization: Bearer share:<token>`, grant manifests validate scoped tokens, and streaming exchanges them for 120-second content-bound tickets. Only SHA-256 token digests are persisted and rehydrated; grant or collection deletion atomically removes durable verifiers and immediately revokes live access tokens and stream tickets. The primary stream route serves confined ranged media bytes, while reusable query-token requests are rejected. Hermetic Rust, loopback HTTP, and Vitest regressions cover persistence rollback, restart rehydration, revocation, URL rejection, wrong-content replay, and delegated byte streaming. |
| `slskdN` incoming filter and blacklist regex timeouts (`efffb49d4`, `fedc1e9e7`) | Partial/differential | Controller compatibility now exposes the sibling's user-supplied backtracking-regex matchers on incoming-search, share-filter, and username-blacklist paths. A .NET-compatibility normalization layer and dual-target differential cover lookaround, backreferences, atomic groups, capture/assertion conditionals, explicit-capture mode, mixed capture numbering, anchors, control escapes, invalid foreign syntax, invariant Unicode case folding, case mode, and live Soulseek decisions. Runtime engine errors fail closed. Exact slskdN 250 ms wall-clock timeout behavior is not yet reproduced: `fancy-regex` currently uses a deterministic backtrack limit instead, while frozen slskd has no corresponding timeout on these paths. |
| `slskNet.Runtime` legacy peer path encodings (`192c5e4a`) | Implemented/needs-live-proof | Protocol strings now distinguish UTF-8, Windows-1251, and Latin-1; peer file entries, folder requests, and transfer requests retain that metadata and reproduce legacy wire bytes. The daemon keeps a bounded per-peer path-encoding registry learned from search and browse responses, then reuses it for folder browsing and transfer negotiation. Hermetic tests cover exact wire bytes, share-list parsing, registry learning, and outbound folder/transfer reuse. Remaining: required live interop with legacy peers. |
| `slskdN` persisted ignored wishlist result folders (`9f2826466`) | Implemented/hermetic | Ignored peer/directory rules are bounded, normalized, deduplicated, persisted, rehydrated, and exposed through wishlist-scoped CRUD routes. Adding a rule suppresses retained matching results, and future peer responses are filtered before storage. The Rust wishlist workspace embeds each item's rules and provides keyboard-accessible add and restore controls, with responsive layout and explicit future-result semantics. Hermetic daemon, renderer, WebAssembly, and UI tests cover persistence, rollback, duplicate rules, deletion, management controls, and existing/future result suppression. |
| `slskdN` wishlist edit/search/download policy (`8a244699a`, `f96ef06a1`, `47bd6c599`, `d91ea7b5c`, `73767889b`, `ac3c08101`, `ffddeff27`, `a1984f8f9`, `9a06ffa8c`) | Implemented/hermetic | Filter text, enabled state, auto-download state, maximum results, nullable maximum downloads, viewed/search timestamps, hit counters, totals, and the last search identity are bounded, persisted, rehydrated, and mutable through the API. Incoming results enforce the literal include/exclude filter and per-item result cap, hide locked hits, retain ignored/filtered/locked counters, and expose bounded paged item history. Manual item searches dispatch to the network; scheduled expirations and explicit completions finalize counters. Auto-download ranks a best peer-directory group, enqueues its files through the normal confined transfer path, records the count, and disables one-shot or limit-reached items. The Rust workspace now exposes a responsive run ledger, unseen-result state, editable policy controls, mark-viewed action, and on-demand bounded history. Hermetic daemon, renderer, native Rust, and WebAssembly gates cover restart state, filtering, counters, history/viewed routes, best-folder enqueue, auto-disable, and the UI contract. |
| `slskdN` transfer race/optional-port fixes (`e22877f25`) | Implemented/hermetic | Server decoders accept omitted obfuscation fields and treat zero or out-of-range type-1 obfuscated ports as absent while retaining the valid plain endpoint; protocol regressions cover both 16-bit peer-address and 32-bit connect-to-peer forms. The sibling's late-fault observation change is .NET task-specific: Rust transfer reads/writes are directly awaited inside bounded `tokio::time::timeout` futures, with no detached race participant whose exception can go unobserved. Existing loopback tests cover transfer error propagation and direct, obfuscated, indirect, and plain-fallback paths. |
| `slskdN` bounded streaming fallback cache and forwarded async reads (`e2289348e`) | Implemented/hermetic | The primary `/api/v0/streams/:contentId` route now streams confined local-share or completed-download files through direct Tokio async reads in bounded 64 KiB chunks, with per-I/O deadlines, single-range `206` support, and `416` rejection for invalid or multipart ranges. The .NET forwarding wrapper and fallback-miss cache are not applicable because Rust reads the opened file directly and performs no fallback scan/cache; preview-ticket storage remains bounded. Hermetic writer and loopback HTTP range tests cover the behavior; live browser/player interoperability remains unproven. |
| `slskdN` peer and mesh preview streams (`617033e47`, `b6f3b54be`, `86872bc31`, `8880c3a8b`, `8928aef73`, `a58bf7f8d`) | Implemented/live mesh proven | Two-minute peer/mesh tickets are bounded, audio-only capabilities; ticket creation remains authenticated while ticket redemption is anonymous, cache-disabled, globally concurrency-limited, and non-rangeable. Local share/completed-download tickets stream confined bytes directly. Remote peer tickets resolve a fresh bounded peer endpoint, negotiate the normal direct/obfuscated Soulseek transfer, and pipe bytes to the HTTP response without creating a transfer record or local file. Remote mesh tickets require exact size and SHA-256 and use either an authenticated explicit HTTP range endpoint or a configured peer-ID/username/endpoint/certificate-pin record. The HTTP path rejects redirects, credentials, and special-use targets; the overlay path authenticates a signed hello, verifies the pinned TLS 1.3 certificate and expected remote username, calls frozen `MeshContent/GetByContentId` in 32 KiB ranges, and verifies the assembled file before exposure. Both paths use confined transient files and clean up on success or failure without deleting pre-existing files. Hermetic raw HTTP, Soulseek peer-protocol, HTTP range-source, pinned overlay, hash-failure, and cleanup tests cover all byte paths. The 2026-07-16 live matrix resolved slskdN's real SHA-256 content ID and fetched the exact 42-byte FLAC fixture through the pinned `MeshContent` service with a matching payload hash. An indirect-only Soulseek preview proof remains optional. |
| `slskdN` bounded dashboard and messaging polling (`46a58cc1`, `a1255f02`, `2783158e`, `1860dfd6`, `d46a1ad7`, `e2487ccb`) | Implemented/hermetic | The canonical inventory now includes `GET /network/stats`, `GET /rooms/activity`, and `GET /conversations/activity/unacknowledged`. The Rust daemon projects each response from bounded local stores; peer-heavy network arrays are omitted unless `includePeers=true`, room activity excludes local messages and non-joined rooms, and unread activity is a scalar. Focused route tests cover exact shapes and versioned aliases. Rust UI ownership tests already enforce one active workspace and no hidden-pane polling fan-out. The .NET SQL count/index changes are storage-engine-specific. |
| `slskdN` incremental polling and bounded hydration delta (`18f2699f5` through `3fcb7ff8b`) | Implemented/hermetic | Stable millisecond update cursors now cover transfer changes, private conversations, joined-room messages, and durable bounded Pod channel messages; successful directional transfer history is stably paged; room and Pod rows have stable message IDs; swarm analytics dashboard/performance/rankings/efficiency/trends/recommendation routes share bounded snapshots; and port-forwarding available-port hydration caps explicit pages at 1,000 while excluding live rules. Focused routes cover aliases, cursor/range rejection, restart durability, retention, snapshot semantics, ranking limits, and forwarding-state hydration. The global transfer-speed and member projections already scan bounded Rust stores, while the sibling's hidden-tab/overlap/timer changes are not applicable to the single-active-workspace Rust UI. |
| `slskdN` bounded search-progress hydration (`c2586f576`) | Implemented/hermetic | Search projections expose `responsesAvailable` only when the Rust store has response payloads or the search is terminal. Unlike the sibling's transient count-only progress path, Rust peer responses are bounded and stored immediately, so active payload availability is authoritative. Focused tests cover empty active, active-with-results, and terminal-empty projections; the sibling React hydration guard is not applicable to the Rust-native workspace. |
| `slskdN` adaptive multi-source transfer fixes (`9ca597045`, `c2557f0fd`, `e6a0522db`) | Implemented/hermetic | `POST /api/v0/multisource/swarm` and `/swarm/async` now execute a real bounded swarm over HTTP range sources; the older `/download` route enters the same engine when sources are supplied and otherwise retains its compatibility queue behavior. Requests require at least two unique sources and an expected SHA-256 digest, default a non-positive chunk size to 512 KiB, cap chunk/source counts and memory per response, preflight exact range semantics, dynamically assign pending chunks to the next free source, retry a failed chunk on a different source, assemble in order, verify exact size/hash, and publish without overwriting through a same-filesystem atomic link. Source-less swarm requests first use the durable HashDB/MBID/shadow relation and verified peer capabilities to select range endpoints. Source redirects, URL credentials, special-use network targets, malformed ranges, and unconstrained output paths are rejected; source authorization never enters job projections. The job store handles an empty source set without scoring/averaging and records progress/results for list/detail polling. Hermetic loopback tests prove two peers concurrently supply one verified output, while validation regressions cover the empty-source and zero-chunk guards. Raw Soulseek transfers remain sequential because that protocol exposes start-offset-to-EOF rather than harmless bounded ranges, matching the sibling's stated policy. |
| `slskdN` private-message gate auto-response (`a87768464`) | Implemented/hermetic | Inbound new messages pass through a bounded, literal human-check/share-gate classifier. The feature is opt-in through sibling-compatible `SLSK_PRIVATE_MESSAGE_AUTO_RESPONSE*` startup configuration and a bounded runtime settings endpoint, validates reply and cooldown bounds, keeps a capped case-insensitive per-peer cooldown registry, dispatches through the live server session, records the outbound message, and redacts reply text from diagnostics and API responses. Disabling it clears live cooldown state. The Rust Messages workspace exposes the enabled toggle, cooldown, optional replacement reply, and explicit runtime-only persistence state. Hermetic daemon, renderer, native Rust, and WebAssembly tests cover candidates, false positives, oversized input, cooldown, capacity, live mutation, reset, configuration bounds, redaction, and UI controls. |
| `slskdN` transfer request metadata and single-pane failure UX (`683aeee20`, `0076a542d`, `ad4c8d4e`, `b2f68ed17`, `47bd6c599`) | Implemented/hermetic | Downloads now have a stable UUID request identity across source replacements, bounded rename/cancel/detail/list endpoints, aggregate request state, durable attempt history, and atomic SQLite projection/event writes. Queue requests retain destination, batch, bitrate, sample rate, bit depth, length, artist, album, title, track, and year metadata; successful MP3, FLAC, and WAV downloads also receive bounded header-derived technical metadata. A confined completed-path template supports uploader, remote folder/parent/name, batch, request name, and date tokens. The Rust Downloads workspace renders one request row with an attempt spine, metadata columns, per-device persistent column visibility, attempt history, rename/cancel controls, and bounded failure-code recovery actions without exposing internal errors. Focused daemon, migration rollback, redaction, native renderer, WebAssembly, desktop/mobile browser, strict Clippy, endpoint-drift, and frozen-classification gates cover the behavior. |
| `slskdN` automatic failed-download retry (`76684f853`, `35bdec282`, `5cebdb63c`, `f5a49bc50`, `cb9940d97`, `dd87b7fe1`, `82368a51a`, `631902e2a`) | Implemented/hermetic | A connected-session worker scans only aged, retryable failed audio downloads and creates metadata-preserving attempts under the stable request identity. Startup configuration matches the frozen defaults and validates the retry delay, scan interval, process-lifetime attempt budget including unlimited mode, global/per-peer cycle caps, peer cooldown, alternate-source search budget, and size tolerance. The planner is oldest-first, latest-attempt-only, active-slot-aware, and never retries rejected or cancelled transfers. It ranks unlocked cached alternatives by availability, speed, and queue depth; when needed it dispatches a bounded global basename search, waits a scan interval, then uses an eligible alternate or falls back to the original peer. Transfer and search dispatch happens only after persistence; conditional rollback preserves concurrent queue/search allocations. Process-lifetime retry tracking is pruned with bounded transfer history and expired peer cooldowns. Hermetic tests cover defaults and invalid bounds, attempt/cooldown/per-peer limits, metadata continuity, deduplication, network discovery, size-tolerant alternate selection, tracker pruning, and concurrent rollback safety. |
| `slskdN` underperformance rescue detector and sidecar exclusion (`063317d64`) | Implemented/hermetic | A connected-session worker evaluates only the latest active audio attempt for the sibling-default queued-too-long, low-throughput, and stalled conditions. Configuration is bounded, per-cycle work and repeat activation are capped, and artwork and other sidecars use the shared audio allowlist. The worker first resolves the sibling-compatible lowercase-basename/size HashDB key and requires a consistent whole-file SHA-256 plus at least two verified mesh range peers; it downloads to a distinct confined path, verifies exact size/hash, and only then cancels the original and creates a succeeded metadata-preserving attempt under the stable request identity. The original partial file remains untouched, stale or failed swarms do not cancel it, and `byteHash` is never misused as a whole-file digest. Cached Soulseek alternatives remain the fallback and are ranked and size-checked before atomic source substitution; otherwise the worker issues a bounded global basename search. Soulseek cancellation is enforced before startup, at chunk boundaries, and during progress/final projection. Transfer locking covers durable promotion/substitution and persistence rollback. Hermetic planner, source-swap, cancellation, configuration, sidecar, filename-key, and two-peer automatic swarm regressions cover this detector. |
| `slskdN` transfer speed telemetry and non-negative completion speed (`b2ebabc43`) | Implemented/hermetic | Transfer attempts retain their actual start timestamp and resume offset, then project non-negative average speed, elapsed time, remaining time, and terminal end time instead of fixed placeholders. Restarted attempts reset the measurement origin to their persisted byte offset. Live download/upload speed totals, current slskdN speed aliases, telemetry summaries, and per-user leaderboards derive from measured attempt speeds; persisted transfer rows retain the actual start when available. Hermetic regressions cover exact resumed-offset math, legacy state decoding, aggregate aliases, arithmetic means, and restart reset behavior. |
| `slskdN` regular-first compatibility dials (`369f73169`) | Implemented/hermetic | Soulseek obfuscation now has validated `compatibility` and `prefer` startup modes matching the frozen client posture. Compatibility is the default and orders regular before type-1 obfuscated peer-message, browse, negotiation, and file-transfer connections; prefer reverses that order when outbound preference is enabled. Disabling obfuscation removes its listener advertisement and outbound path, unsupported `only` mode fails startup, and both supported modes retain the alternate transport as fallback. Configuration, deterministic ordering, obfuscated-only, and failed-obfuscated-to-plain loopback tests cover the matrix. |
| `slskdN` browse reconnect and unavailable-peer failures (`cc0dc2d0d`, `b5a280088`) | Implemented/hermetic | New browse dispatch is rejected with `503` before mutation unless the Soulseek session is connected. The React browse adapter drives slskr's bounded asynchronous request/status API, reuses completed projections, stops on terminal failure, and renders the daemon's bounded public reason. Versioned browse reads now return the slskd directory shape rather than an internal record shape. Hermetic daemon and Vitest regressions cover disconnected dispatch, cached and newly completed results, unavailable peers, public error rendering, persistence rollback, and direct/indirect browse failures. |
| `slskdN` download destination preflight and timeout classification (`3c837c168`, `e73f72f93`, `624e67a7c`) | Implemented/hermetic | Rust downloads use one confined resumable destination rather than separate incomplete/final move roots. The destination parent is created through descriptor-relative no-follow traversal and the regular file is opened before any transfer offset is sent, so local access failures are terminal and are never retried as remote failures. Every peer lookup, negotiation, token, offset, and chunk I/O is directly awaited under the configured positive peer timeout and projects a bounded failed-transfer reason. Symlink, non-regular path, parent-swap, resume-offset, timeout, and loopback transfer regressions cover the equivalent behavior. |
| `slskdN` non-audio HashDB ingestion guard (`1bd0bfb8f`) | Implemented/hermetic | HashDB compatibility stats and entry projections now admit only the same bounded audio-extension allowlist used by autonomous download retry, excluding artwork and other sidecars from hash counts and rows. A route regression proves an adjacent FLAC is retained while a JPEG sidecar is omitted. |
| `slskdN` HashDB-to-MBID mesh source discovery (`556fe3500`) | Implemented/live content transport proven | A bounded durable Rust content-discovery store validates and atomically merges exact-size hash metadata with optional MusicBrainz recording IDs, and a bounded shadow-index relation resolves recording IDs to peer IDs. The inbound pinned overlay implements the frozen aggregate-only `shadow-index` QueryByMbid/QueryBatch response and exact-range `MeshContent/GetByContentId`; aggregate-only behavior matches the sibling, whose decoder does not recover routable peer IDs from variant hints. Because the frozen capability descriptor serializes neither an HTTP range endpoint nor an overlay certificate pin, bounded operator trust records bind peer ID, Soulseek username, IP-literal overlay endpoint, certificate SHA-256, and an optional HTTP range template. Source-less swarms select trusted and slskr-local HTTP peers while retaining exact range/hash verification, and remote preview tickets consume trusted frozen overlay ranges directly. Hermetic tests prove a mixed trusted/descriptor swarm, aggregate shadow queries, exact inbound ranges, pinned outbound ranges, identity rejection, and cleanup. The live matrix proves the frozen cross-runtime `MeshContent` request and exact response bytes; automatic endpoint/pin distribution would require a versioned wire extension. |
| `slskdN` indexed search history listing (`170ea341b`) | Implemented/hermetic | The SQLite schema creates `idx_searches_created` over descending search creation time and `idx_search_results_search` for child lookup before persisted search listing is exposed. Persistence tests cover ordered paging and restart hydration. |
| `slskdN` MediaCore ContentID example fields (`ca0dbda66`) | Implemented/hermetic | Example buttons populate registration, read-only resolution, and validation inputs with the matching external ID and ContentID. A React regression covers all four visible values. |

### Frozen source-commit registry

The classifications below cover every commit touching `src` at the frozen
heads. More specific rows above override a group's conservative default.
`scripts/check-upstream-parity-classification.sh` always validates the committed
registry's exact counts, prefix identities, format, and uniqueness. When the sibling
repositories are present, it additionally fails when a source/config commit at
either snapshot is absent. Commits that touch only docs, memory, tests,
examples, or release/packaging automation are classified by that script as
non-runtime or gate-backed rather than copied into this ledger individually.

- `needs-proof` behavior default (65): `d46a1ad77` `1860dfd6c` `2783158e1` `a1255f028` `46a58cc14` `1100d415d` `efffb49d4` `fedc1e9e7` `9ca597045` `c2557f0fd` `e6a0522db` `e2289348e` `6ae55d576` `9f2826466` `34113e245` `1bd0bfb8f` `0ed04b456` `9654eac5f` `063317d64` `9a06ffa8c` `649fd40c7` `c6ca8ebb6` `9e277c701` `de82ad218` `8a244699a` `f96ef06a1` `47bd6c599` `d91ea7b5c` `73767889b` `ac3c08101` `683aeee20` `0076a542d` `ad4c8d4ed` `b2f68ed17` `ffddeff27` `a1984f8f9` `a87768464` `556fe3500` `170ea341b` `cb9940d97` `b2ebabc43` `d43687540` `369f73169` `af668195f` `cc0dc2d0d` `b5a280088` `18f816414` `401ac6b42` `3c837c168` `e73f72f93` `36145133c` `90d2a57aa` `57326ef1f` `8a7e3753b` `b781b218c` `d1e9ad7c3` `835fb8d27` `8880c3a8b` `76684f853` `35bdec282` `86872bc31` `b6f3b54be` `a58bf7f8d` `617033e47` `631902e2a`.
- `needs-proof` security/advisory default (37): `081302e43` `0f9799305` `f5a128d1a` `f3b90f12f` `a835151e8` `a7b73bd05` `97b8de60e` `3eaa675a6` `49ed4e89f` `9f59bc111` `459838c8f` `118a5c360` `b13872b7e` `f96d68296` `8944a7751` `6edad2aa4` `34745e114` `1ca284972` `0db03dd8b` `206b4c866` `3088c7eae` `f01388bde` `17f922a21` `dd6f45d1a` `7f909ab2a` `2f4031597` `099ff7952` `dcd980806` `7229ba6b5` `6fcdafd6a` `70e498e08` `643852d09` `8ec8d2aa2` `51c857a81` `88940a84a` `9bd3645d3` `763da549f`.
- `not-applicable` dependency/release/test-only default (23): `5a87a1aa9` `a5b05fba9` `a238c1d5a` `d5fed7d6e` `ca7639348` `e6099f133` `da9c2299b` `baef05557` `51b169ad2` `f57f4d862` `5b6895a50` `178ecfb75` `f904fe10a` `d97e08f6a` `02e21e78c` `22e86d0d1` `40ccd22be` `c828cf873` `2f4b16ffa` `5b4fe84f2` `b35fac890` `d57af6229` `ce61428e7`.
- `not-applicable` .NET/logging/lifecycle default (17): `2be213804` `ee802eb03` `59f1a7fe5` `f837b5111` `e124b2f46` `2540c8332` `01631fc74` `983466e9c` `8490eda02` `8e36ad2e1` `60039cee8` `72bba6c23` `47b0840d3` `f31965fac` `a51e25e3b` `546ac8a5e` `7a2c8cb37`.
- `needs-proof` manual-review default (22): `e2487ccb5` `1eb0a4951` `8928aef73` `4f3d6657a` `1d2f21348` `c6b31866a` `454797904` `cf692c036` `ffce1d305` `ee04c5818` `230549f93` `1bcef0404` `ea4362e2e` `25feed2ee` `c39a604ac` `dd87b7fe1` `82368a51a` `c6105a56e` `bcfcc7319` `5cebdb63c` `f5a49bc50` `765a4db82`.
- `needs-proof` start-of-day behavior additions (2): `ca0dbda66` `624e67a7c`.
- `needs-proof` start-of-day security addition (1): `f9663cba0`.
- `not-applicable` start-of-day test-only additions (2): `6a1b1581c` `8a222ed28`.
- `not-applicable` start-of-day .NET/UI/lifecycle additions (34): `af651569b` `20fd4a5f0` `fd1ad086e` `cfd981f6f` `7f8f6e6e8` `1e1bd949b` `e90d82871` `8ddd93f91` `0ec711cb0` `0d119d69f` `7aa547e58` `f7e0c4a48` `023fd2014` `21630fc36` `05d8335f0` `013adf136` `56718f62c` `2495586e8` `7909d5717` `c07c23791` `deff6900b` `631a5808f` `da0632767` `b149843ff` `9364a9d85` `626f39d54` `730f6e942` `f63c1b2e1` `ec356e40a` `cfd3a563f` `69d318234` `c24c20602` `fc279fc51` `05c63be87`.
- `needs-proof` frozen polling/API additions (7): `18f2699f5` `fa9252898` `48011f973` `6f8bb0596` `88ba55668` `a6bcc2d83` `1e9068dd6`.
- `needs-proof` frozen search hydration addition (1): `c2586f576`.
- `not-applicable` frozen Rust-native/UI polling additions (6): `565898635` `65c367283` `21dae85ae` `6acaa23e0` `d50e337d7` `3fcb7ff8b`.
- `slskNet.Runtime` source registry: `192c5e4a` is implemented/needs-live-proof; `af73ff3f` is not-applicable logging severity; `1fce75fa`, `681c48c9`, `0fa803d5`, and `09b16f96` are not-applicable .NET dependency changes covered by dependency gates.

### Post-freeze current-head delta

The 192 non-merge slskdN commits after the frozen head and through
`db9d6eee2af1b484c62495d4fb3683f7009a15f4` contain 105 documentation/test-only
changes and the following 87 source/config changes. The classification gate validates
this exact range and registry.

| Upstream change | Classification | Current evidence and remaining closure |
| --- | --- | --- |
| UI polling ownership (`d4490193`, `429ae017`) | Implemented/not-applicable | The Rust WebUI has one active route owner, starts hidden RustyMilk stopped, and has no React Bridge, Lyrics, or Lidarr hidden-tab timers. Existing native renderer and WebAssembly tests enforce lazy hidden panes and the absence of hidden polling loops. |
| Bounded security dashboard aggregation (`cd9bd234`) | Implemented/hermetic | Security counts and rows are derived directly from bounded Rust ban, user, event, webhook, and mesh stores. The security dashboard projection test covers state-backed counts; no unbounded .NET singleton event lists exist in the Rust runtime. |
| Passive mesh diagnostics (`e4d2cd0a`) | Implemented/hermetic | Rust mesh status reads bounded local snapshots and does not trigger NAT, DHT, or external status probes. The active-route UI ownership tests prevent hidden diagnostic polling; live mesh work remains in explicit background services. |
| Bounded listen-along directory hydration (`3d04928e`) | Implemented/hermetic | Listening-party and Pod channel projections read bounded room/message/share stores and do not enumerate an unbounded distributed directory. Existing joined-room listening-party tests cover local state and message projection. |
| SQLite library-health dashboard aggregation (`e2d53ea5`) | Implemented/hermetic | `summary`, `dashboard`, paged/filtered `issues`, and type/artist/release/codec group routes now expose the current camel-case DTOs and exact request bounds. Rust library state is capped at 10,000 items, and route tests cover dashboard aggregation, enum filters, large offsets, limited group totals, blank-artist exclusion, the `UNKNOWN` codec bucket, required paths, and rejected limits. |
| SQLite user download-history aggregation (`6debd9f8`) | Implemented/hermetic | `/transfers/downloads/user-stats` now returns the current username-keyed map with exact total/success/failure/byte/time fields from the bounded transfer store. A route regression covers active, failed, successful, and upload exclusion semantics. |
| Batched search-result user groups (`f9f7c450`) | Implemented/hermetic | `/users/:username/group` now returns the current JSON string and `/users/groups` returns a trimmed, case-insensitively deduplicated username map capped at 100. Tests cover custom/default groups, duplicates, blanks, version aliases, and overflow rejection. |
| Indexed MediaCore registry reads (`f534bd51`) | Implemented/not-applicable | Rust content-discovery and content-ID projections are already bounded in-memory maps/vectors with fixed retention; they do not repeatedly deserialize the .NET registry JSON file. Existing content discovery, hash lookup, and MediaCore projection tests cover reads and bounded merges. |
| Streamed auto-retry candidates (`4e749246`) | Implemented/not-applicable | Rust auto-retry scans an already bounded retained transfer queue, orders eligible failures stably by update time and ID, enforces global/per-peer/active-slot caps, and stops plan construction at the configured limit. Planner and cycle tests cover stable selection, attempt/cooldown limits, latest-attempt filtering, metadata preservation, and tracker pruning. |
| Bounded round-robin shadow-index publication (`4138a022`) | Implemented/live-transport-proven | slskdN pages normalized recording IDs with a stable keyset cursor and publishes through its corrected distributed client. slskR snapshots at most 96 bounded durable shadow records per cycle, encodes the frozen MessagePack shard/timestamp shape, uses full routable UTF-8 peer identities while rejecting legacy eight-byte pseudonyms as routes, and republishes every 30 minutes. Cross-runtime vectors cover the key and payload; the live signed Store proves the transport/admission path. |
| Scheduled and batched search-history cleanup (`cb8750c8`, `c79dc59d`) | Implemented/not-applicable | Rust never accumulates an unbounded SQL search history to scan or delete periodically. The live search store is hard-capped at 500 records, rejects creation when every retained search is active, otherwise evicts exactly the oldest non-active record during insertion, and deletes that record from persistence in the same workflow. Startup hydration also caps retained records and aggregate results. Capacity, deterministic eviction, hydration, persistence rollback, and auto-retry tracker pruning tests cover the bounded design, so the sibling's cleanup interval, non-overlap latch, and 250-row EF delete batches are storage-engine-specific. |
| Single-owner mesh descriptor refresh (`ee6309fc`) | Implemented/not-applicable | Rust has no duplicate hosted peer-descriptor publishers: signed Soulseek capability descriptors are generated on demand for `KSDN` acknowledgements, one bounded Mainline-DHT task owns rendezvous announce/lookup, and one bounded mesh-DHT publisher owns catalog refresh. |
| Batched Pod discovery refresh (`ef11303f`) | Implemented/live-transport-proven | slskdN queries one listed-Pod snapshot, refreshes eligible metadata, and performs one shared-index read/write per cycle. slskR snapshots at most 32 public Pods, emits compatible Pascal-case metadata under `pod:metadata:{id}`, and writes one `pod:index:listed` record per cycle through the live-proven signed Store path. |
| Batched backfill peer-count reads (`e58387bc`) | Implemented/live-proven | Rust derives one bounded, deduplicated FLAC candidate snapshot from the capped search store and reads case-insensitive daily peer counters from a bounded in-memory map, so it has no per-candidate database query. The counters persist atomically across restart and reset by UTC day. The sibling-compatible config, stats, enable, idle/busy, trigger, candidates, and file routes execute a two-connection, ten-per-peer scheduler; the peer probe uses normal transport negotiation, validates the exact size and FLAC STREAMINFO header, hashes at most the first 32 KiB, and durably merges the sibling-compatible filename/size key and byte hash. Parser, route, queued-token, and restart tests cover the policy, and the current-client matrix proves a live queued slskR-to-slskdN header exchange with the exact expected byte hash. |
| Batched content peer-hint indexes (`c48c5b34`) | Implemented/live-transport-proven | slskdN coalesces a bounded 32-item content-ID batch and performs one reverse peer-content index read/write. slskR publishes at most 96 deduplicated MusicBrainz content IDs with the compatible MessagePack content-peer shape and one compatible MessagePack reverse index through the live-proven signed Store path. |
| Deferred search-response hydration (`603e05e3`) | Implemented/not-applicable | Rust wishlist and automatic-alternative workflows observe completion in the already bounded in-memory `SearchStore`; no EF query deserializes a response graph on each polling iteration. Auto-retry waits one configured scan interval and reads eligible alternatives once, while wishlist completion is projected directly from the peer response and terminal search state. Existing scheduler, response, and alternative-selection tests cover these paths. |
| Direct advertisable content-hint query (`ddb8fcc4`) | Implemented/not-applicable | Rust's mesh-DHT publisher derives a bounded, deduplicated MusicBrainz content-ID snapshot directly from the capped durable content-discovery store; it does not hydrate a share-file graph or expose a separate blocked/quarantined mapping that could leak through that query. Publication vector tests and the live signed Store path cover the compatible content-peer records. |
| Set-based Pod history deletion (`4a4ddfb3`) | Implemented/hermetic | Pod deletion and channel removal delete their bounded durable message history through the atomic Pod store workflow, with conditional rollback if persistence fails. Route and store regressions prove that only the deleted Pod or channel history is removed and that failed persistence restores the prior state. The sibling's EF `ExecuteDeleteAsync` optimization has no Rust SQL analogue. |
| Set-based wishlist viewed update (`69ac723f`) | Implemented/hermetic | `mark-all-viewed` mutates one bounded wishlist snapshot and persists all changed rows in one SQLite transaction; conditional rollback preserves the prior snapshot on failure. Item-level and all-item route tests cover timestamps, unseen-state clearing, persistence, and rollback. |
| Serialized library-health UI polling (`ee9fea91`) | Implemented/not-applicable | The Rust UI has one active route owner and no React interval/effect pair that can overlap library-health requests. Native renderer and WebAssembly ownership tests enforce lazy hidden panes; daemon health projections read one bounded library snapshot per request. |
| Reused VirtualSoulfind intent batches (`02ceaec3`) | Implemented/live-proven | The Rust v2 queue is capped at 1,024 track intents and atomically claims only `Pending` work before processing, so a second claim cannot execute the same intent. Focused tests cover exact-once claims and statistics; the current-client matrix proves the same create/plan/process/failure workflow against both clients. |
| Streamed scheduled file pruning (`f8d6f613`) | Implemented/not-applicable | Rust does not materialize a recursive file list before deletion: confined transfer cleanup operates on bounded transfer projections and direct filesystem operations, while transient preview staging files are removed at workflow completion. The sibling change only replaces `GetFiles` plus repeated enumeration with `EnumerateFiles`; it adds no externally visible retention behavior. |
| Batched library-health release checks (`f3594441`) | Implemented/not-applicable | Rust library-health summaries and release groups are computed from one capped 10,000-item library snapshot and never issue per-track HashDB queries. Existing grouping and scan-route tests cover bounded release aggregation. The sibling's EF/SQLite recording-ID batch lookup is a storage-specific optimization. |
| Batched source-discovery ingestion (`962dfa7a`) | Implemented/hermetic | The full source-discovery API starts and stops a bounded continuous global search, retains at most 32 cycle tokens in the capped search store, deduplicates peer/file/size observations, and projects status, size/filename lookups, summaries, partial-support state, and statistics. Incoming Soulseek responses are already appended under one search-store write lock and persisted as one bounded snapshot rather than issuing a SQLite command per row. Focused routes cover dispatch, overlap rejection, source projection, summaries, and stop statistics. |
| Lock-free signal deduplication (`31a22f7e`) | Implemented/not-applicable | Rust fan-out uses Tokio's bounded broadcast channel, which gives every subscriber an independent receiver cursor and never serializes receipt behind a shared mutex. Lagging consumers receive an explicit bounded lag error, and WebSocket tests cover broadcast delivery, lag handling, and closure. The sibling's `ConcurrentDictionary.TryAdd` conversion is a .NET signal-bus implementation detail. |
| Batched library-Bloom album analysis (`6761751e`) | Implemented/not-applicable | Rust's library, content-discovery, and MusicBrainz projections operate on capped in-memory snapshots and do not fetch one HashDB track list per release. Completion and coverage routes group the bounded snapshot in one pass. The sibling change replaces per-release SQLite reads and nested membership scans with one batched query and lookup; it adds no wire or API behavior. |
| Batched album-completion evidence (`0ef2adaf`) | Implemented/not-applicable | Rust MusicBrainz completion derives every artist/item completion row from one capped library snapshot and does not perform per-release track or per-recording HashDB queries. Existing completion and coverage route tests exercise the one-pass projection; the sibling change is a SQLite query batching optimization. |
| Indexed Music recording lookup (`fc41d773`) | Implemented/not-applicable | Rust VirtualSoulfind v2 derives deterministic track IDs from the capped library snapshot, while content discovery retains at most 16,384 normalized hash records. It does not scan album-target tables to resolve one recording. Catalogue stability, local planning, and bounded content-discovery tests cover the equivalent lookup behavior; the sibling index/query-plan change adds no API contract. |
| Bounded recent Music lookup (`28dfa992`) | Implemented/not-applicable | Rust's VirtualSoulfind catalogue is derived from the capped 10,000-item library snapshot, and every catalogue list applies an explicit maximum before serialization. It has no album-target query fan-out or per-track HashDB lookup. Catalogue route and bound tests cover the Rust-native behavior; the sibling change is an indexed SQLite batching optimization. |
| Batched music metadata matching (`eb06ba0d`) | Implemented/not-applicable | slskR resolves catalogue and content-discovery data from capped in-memory library and hash snapshots, groups candidates in one pass, and applies explicit result bounds before serialization. It has no per-release or per-recording SQLite query loop; catalogue stability, content lookup, and bounded projection tests cover the Rust-native path. |
| Batched library-health remediation state (`09a456e5`) | Implemented/not-applicable | Rust library-health scans and remediation operate on a single capped library snapshot and persist bounded job records atomically. The sibling change replaces repeated SQLite issue scans and per-row updates with batched queries and transactions; it adds no API or remediation-state behavior absent from slskR. |
| Batched passive FLAC inventory ingestion (`1edbe412`) | Implemented/not-applicable | slskR ingests bounded search/share observations under one store write, deduplicates candidates in memory, and persists one bounded snapshot rather than issuing a database command per file or peer. Source-discovery and backfill tests cover deduplication, limits, statistics, and the live FLAC header/hash path. |
| Collapsed HashDB peer-state writes (`7e65d50a`) | Implemented/not-applicable | Rust peer capability and observation state is upserted directly in bounded maps and atomically persisted; there is no preliminary SQL read/create operation to collapse. Capability persistence and source-discovery tests cover creation, update, normalization, and restart behavior. |
| Batched HashDB mesh merge (`47a2cf03`) | Implemented/not-applicable | `ContentDiscoveryStore::merge_hash_entries` validates an entire bounded batch before taking one write path, resolves exact keys in its capped in-memory index, rejects conflicts without replacing local evidence, assigns local sequence IDs, and persists one atomic snapshot. The sibling change removes per-entry SQLite reads and writes but adds no external merge behavior absent from Rust. |
| Batched album-target track writes (`c1ea4a00`) | Implemented/not-applicable | Rust library and MusicBrainz catalogue state is updated inside capped in-memory stores and persisted as bounded snapshots; it has no per-track SQLite insert loop. Catalogue and completion tests cover normalized track ordering and one-pass projection. |
| Aggregated HashDB statistics snapshot (`e0f9e64f`) | Implemented/not-applicable | Rust derives HashDB and content-discovery statistics from one bounded store read, so it already avoids the sibling's repeated scalar SQL queries. Route tests cover the state-backed counts. |
| Batched release-job status writes (`c94e49ff`) | Implemented/not-applicable | Rust discography and label-crate jobs retain bounded release arrays in their job records and persist each job snapshot atomically; it does not issue one SQL upsert per release. Job normalization and route tests cover the equivalent externally visible state. |
| Checkpointed library-health progress (`58df6e39`) | Implemented/not-applicable | Rust library-health scans operate on one capped in-memory library snapshot and persist bounded job state atomically; they do not issue one database upsert per scanned file. Existing scan, aggregation, and persistence tests cover the externally visible progress and terminal state. |
| Consolidated history-backfill ingestion (`b945b901`) | Implemented/not-applicable | Rust derives a bounded, deduplicated FLAC-candidate snapshot from its capped search store and merges it through one atomic content-discovery store write, so it has no per-search database ingestion loop to consolidate. Backfill route, candidate, deduplication, persistence, and live-header tests cover the equivalent externally visible workflow. |
| Skipped unchanged job aggregate writes (`e886546d`) | Implemented/not-applicable | Rust discography and label-crate job records retain bounded release arrays, derive aggregate state in memory, and persist only explicit job mutations as atomic snapshots; read projection does not perform an unconditional SQL write. Job normalization and route tests cover the same externally visible aggregate state. |
| Batched discography-coverage evidence (`2c803c92`) | Implemented/not-applicable | Rust MusicBrainz coverage derives releases, tracks, wishlist matches, and available hashes from one capped library/content-discovery snapshot rather than issuing per-release or per-track SQL lookups. Coverage and completion route tests exercise the equivalent one-pass projection. |
| Batched canonical-audio statistics (`1852ced6`) | Implemented/not-applicable | Rust canonical and variant projections group the capped content-discovery snapshot in memory and persist bounded snapshots atomically; they do not perform per-profile canonical-stat queries or upserts. Canonical selection, variant grouping, and bounded projection tests cover the externally visible results. |
| Batched Wishlist item inserts (`596cf6d2`) | Implemented/hermetic | Rust collects Wishlist mutations in its capped in-memory snapshot and now upserts 40 items per SQLite statement inside one transaction, staying below SQLite's parameter limit. The same batch writer serves bulk imports, mark-all-viewed, source-feed seeding, and MusicBrainz promotion paths. A 41-item regression crosses the statement boundary and verifies both insert and conflict-update behavior. |
| Batched MusicBrainz Wishlist promotion (`397b421b`) | Implemented/not-applicable | Rust MusicBrainz coverage, Bloom, and Wishlist projections derive candidate and existing-key sets from capped in-memory snapshots, deduplicate them in one pass, and persist resulting Wishlist rows through the 40-row atomic batch writer rather than awaiting one database insert per promotion. Existing coverage/Bloom route tests plus the cross-boundary batch regression cover the equivalent state and persistence behavior. |
| Batched analyzer migration updates (`566a19b5`) | Implemented/not-applicable | Rust canonical-audio, variant, and library-health projections are recomputed from capped in-memory snapshots and persisted as bounded state; there is no row-by-row analyzer-version migration or per-variant SQLite update loop. Canonical grouping, quality summaries, and library-health scan tests cover the externally visible projections. |
| Batched library-item hash evidence (`ea1e5a66`) | Implemented/not-applicable | Rust library and share projections read capped in-memory snapshots and do not resolve each item through a separate HashDB query. Content-discovery hashes are already indexed by bounded maps, while library listing applies explicit page bounds before serialization. Library list/browser and content-discovery lookup tests cover the externally visible evidence and paging behavior. |
| Indexed library-browser directories (`34e893b0`) | Implemented/not-applicable | The sibling replaces repeated directory-prefix scans with one temporary index. Rust retains at most 10,000 library records under one lock and pages the browser projection directly from that bounded snapshot; it has no Soulseek directory graph or nested LINQ rescans to optimize. The existing browser bound and paging regressions cover the Rust storage model. |
| Batched reconciliation copy state (`2c1551e8`) | Implemented/not-applicable | Rust VirtualSoulfind and library-health projections derive local availability from one capped library/content-discovery snapshot. They do not issue separate local-file and verified-copy SQL reads for each track, so the sibling's bounded `IN` query removes a storage-engine fan-out that does not exist in Rust. |
| Batched upgrade-track hydration (`84b876a2`) | Implemented/not-applicable | Rust replacement and upgrade candidates retain their bounded track metadata in the in-memory catalogue and transfer projections. Candidate projection does not hydrate each track through a separate catalogue query; existing replacement-policy and catalogue tests cover stable selection and bounded output. |
| Batched release-gap evidence (`b84a1304`) | Implemented/not-applicable | Rust MusicBrainz coverage and library-health grouping traverse capped library/content-discovery snapshots once and index their intermediate evidence in maps. There are no per-release track, release-group, artist, or copy-state database calls to batch. Coverage and completion tests exercise the equivalent bounded projection. |
| Paged verified-copy hydration (`239cd41b`) | Implemented/not-applicable | Rust does not maintain the sibling's relational verified-copy table. Upgrade and orphan projections use bounded library and content-discovery snapshots and apply candidate limits before serialization, so they neither load every verified-copy row nor perform per-file lookups. |
| Batched share-grant membership reads (`a38fd7aa`) | Implemented/not-applicable | Rust share grants and share-group memberships are capped in-memory stores protected by one read lock, and grant projection uses direct map/vector membership rather than an EF repository query per group grant. Share-group and grant route tests cover normalization, membership, bounds, persistence, and revocation. |
| Batched warm-cache popularity writes (`b9485962`) | Implemented/not-applicable | The sibling replaces up to 100 individual SQLite popularity increments with bounded multi-row upserts. slskR deliberately reports warm-cache hints as disabled and has no popularity table or per-hint write loop, so there is no equivalent storage fan-out to batch. |
| Set-based warm-cache eviction (`5b45600c`) | Implemented/not-applicable | slskR has no enabled warm-cache metadata store or eviction workflow; the compatibility route returns the sibling-compatible disabled response. The sibling's windowed SQLite delete replaces per-entry metadata deletion without adding an active Rust behavior. |
| Bounded MediaCore variant sampling (`528fe1e6`) | Implemented/not-applicable | Rust MediaCore variants live in a capped content-discovery store and all projection routes apply explicit result limits before serialization. They do not enumerate recording IDs and issue one HashDB read per recording. Variant grouping, canonical selection, and bounded projection tests cover the Rust path. |
| Bounded music-variant fallback scans (`02661683`) | Implemented/not-applicable | Rust VirtualSoulfind fallback resolution scans capped in-memory library/content-discovery indexes and applies bounded candidate limits; it has no per-recording SQLite hydration loop. Catalogue stability and bounded content-discovery tests cover fallback selection. |
| Indexed recommendation Wishlist lookup (`ba6d8691`) | Implemented/not-applicable | Rust Wishlist state is capped, held in memory under one lock, and case-insensitive recommendation deduplication operates directly on that bounded snapshot. It does not load an EF list or perform a SQLite search-text query, so the sibling's new collation index has no Rust storage analogue. |
| Atomic warm-cache touches (`1f2e0603`) | Implemented/not-applicable | slskr has no enabled warm-cache metadata table or read-modify-write touch path; its compatibility endpoint reports the feature as disabled. The sibling's single-statement timestamp update therefore has no Rust storage analogue. |
| Bounded persisted peer ranking (`7754b241`) | Implemented/not-applicable | Rust swarm history retains at most 256 jobs with at most 16 sources each, derives peer metrics from that bounded snapshot, and applies the requested ranking limit after deterministic sorting. It does not hydrate an unbounded relational peer-metrics table. |
| Auto-retry migration registration (`0604a0ec`) | Implemented/not-applicable | slskr creates and migrates its SQLite transfer schema through its own centralized schema path and has no .NET migrator registry or EF auto-retry index migration to register. Transfer auto-retry queries remain bounded by configured per-cycle limits. |
| Bounded download-request attempt hydration (`f7c641d9`) | Implemented/not-applicable | slskr retains a configured bounded transfer history and derives request attempt counts/current attempts from that capped in-memory snapshot. It never hydrates an unbounded relational attempt set, so the sibling's covering index and aggregate/current-attempt query split have no direct Rust storage analogue. |
| Consolidated search startup snapshots (`3adb1b94`) | Implemented/not-applicable | slskr's search store is capped at 500 records, startup hydrates it once from one bounded persistence snapshot, and each WebSocket subscriber receives the same bounded in-memory projection. It has no separate controller/hub database reads to consolidate. |
| Paged native job aggregation (`93672e92`) | Implemented/hermetic | Rust job projections are backed by capped search/controller snapshots and already validate `limit`/`offset`, apply filtering and deterministic ordering before the bounded page is serialized, and expose `total`/`has_more` without hydrating an unbounded relational job set. Route regressions cover page bounds and empty/missing jobs. |
| Skipped discarded manifest contact reads (`2ba47cdb`) | Implemented/not-applicable | Rust share manifests derive ownership and items directly from the bounded collection/grant stores and emit null peer/contact ownership fields. They never enumerate contacts for an application-user owner that cannot be resolved to a peer identity. |
| Batched share-group contact lookup (`42f0b8ac`) | Implemented/not-applicable | Rust share-group members and contacts are capped in-memory stores; member projection takes bounded snapshots and resolves peer IDs directly without issuing one persistence query per member. Membership/contact regressions cover nickname and peer projection. |
| Indexed ignored-Wishlist lookup (`4a029c21`) | Implemented/not-applicable | Ignored results are retained inside capped Wishlist item snapshots and matched directly by normalized username/directory during mutation. slskr has no relational ignored-result table or per-item list hydration requiring a new index. |
| Atomic set-based announced collection replacement (`e47f60ba`) | Implemented/hermetic | Incoming share/collection state is normalized into bounded snapshots and persisted through atomic replacement rather than per-item relational deletes. Collection, item, grant, persistence rollback, restart, and revocation regressions cover all-or-nothing externally visible state. |
| Atomic collection/share deletion and peer-member removal (`47df29fd`, `cb9829d2`) | Implemented/hermetic | Collection deletion holds the collection and grant write locks, transactionally deletes tokens, grants, items, and the collection, and restores both projections on persistence failure. Share-group membership snapshots and group deletion are transactionally replaced/deleted, with route-level rollback regressions for failed persistence. |
| Batched reorder and atomic collection append (`6a144421`, `c21ff298`) | Implemented/hermetic | Collection mutations execute under one bounded store write lock, normalize exact item identities/order, and persist the entire ordered snapshot in one SQLite transaction. Duplicate, reorder, rollback, restart, and atomic snapshot regressions cover the externally visible semantics without EF-specific SQL batching. |
| Atomic share-group admission and stable failure boundary (`6aeb6106`, `f580b6e3`) | Implemented/hermetic | Case-insensitive member admission and capacity checks occur under one write lock; the complete group/member snapshot is committed transactionally. Persistence errors roll back the in-memory projection and return a bounded service-unavailable response, preserving a stable public failure boundary without exposing storage exceptions. |
| Scoped collection item/grant/stream authorization (`1e09a3df`, `0a44bf69`, `74552947`, `6976c606`) | Implemented/not-applicable | slskr performs exact collection, item, grant, and group-member checks against capped in-memory stores under read locks rather than loading unbounded EF collections. SQLite hydration has collection/grant/member indexes, while stream authorization walks only the already-bounded target collection. Access, revocation, missing-content, ticket, and persistence regressions cover the same decisions. |
| Bounded recording-variant fallback (`c8d059b8`) | Implemented/not-applicable | Recording-to-hash candidates live in the capped 16,384-entry content-discovery store and merges accept at most 1,000 entries. Canonical and dedupe projections filter that bounded snapshot directly and preserve deterministic local-confidence metadata, so the sibling's new SQL `LIMIT 1` optimization does not expose an unbounded Rust query path. |
| Bounded canonical-skip lookup (`a0f9c4b3`) | Implemented/not-applicable | slskr accepts explicit verified swarm requests and does not run slskdN's automatic canonical-quality skip workflow. Its canonical and variant projections filter the capped 16,384-entry content-discovery snapshot directly, so there is no all-variants SQL hydration to replace with a best-row query. |
| Batched discovery peer metrics (`947c6965`) | Implemented/not-applicable | Rust swarm history retains at most 256 jobs with at most 16 sources each and derives all peer metrics and deterministic rankings from one bounded snapshot. Source discovery likewise snapshots its bounded observations once; neither path issues one persistence query per peer. |
| Cached IPLD validation membership (`f5c9f015`) | Implemented/hermetic | IPLD validation snapshots the capped ContentID registry and link records once, builds one membership set, and reports distinct broken targets and orphaned sources from that snapshot. Focused route coverage proves valid, broken, orphaned, and inbound-link projections without repeated registry reads. |
| Reused fuzzy-match descriptors (`d64bde7b`) | Implemented/hermetic | Perceptual comparison and fuzzy search snapshot the capped descriptor store once, reuse the target descriptor for every candidate, prefer Chromaprint numeric hashes, apply the sibling's Hamming and 0.7/0.3 perceptual/text scoring, and return deterministically ranked reasoned matches. Focused route coverage proves non-zero direct and multi-candidate results. |
| Bounded fuzzy edit-distance memory (`98f18288`) | Implemented/hermetic | Rust Levenshtein similarity uses two rolling rows sized to the shorter Unicode-scalar input instead of a full matrix. Focused symmetry coverage exercises highly uneven long inputs, while the MediaCore text and fuzzy-search route tests prove the externally visible score. |
| Trimmed fuzzy edit-distance affixes (`57e6b383`) | Implemented/hermetic | Rust removes shared Unicode-scalar prefixes and suffixes before filling its rolling rows while retaining the original maximum length as the normalization denominator. Focused long-prefix and shared-prefix/suffix vectors prove exact scores. |
| Shared MediaCore registry snapshot (`db9d6eee`) | Implemented/not-applicable | Rust MediaCore dashboard, registry, descriptor, fuzzy, IPLD, perceptual, portability, and publishing statistics are derived under one bounded content-discovery/library/share/controller-feature snapshot per request. It does not call separate registry services for the registry and IPLD sections, so the sibling's new shared task removes a fan-out absent from the Rust projection. |

### Post-freeze current-head source-commit registry

- `implemented` slskdN delta (87): `d4490193` `cd9bd234` `e4d2cd0a` `3d04928e` `e2d53ea5` `429ae017` `6debd9f8` `f9f7c450` `f534bd51` `4e749246` `4138a022` `cb8750c8` `c79dc59d` `ee6309fc` `ef11303f` `e58387bc` `c48c5b34` `603e05e3` `ddb8fcc4` `4a4ddfb3` `69ac723f` `ee9fea91` `02ceaec3` `f8d6f613` `f3594441` `962dfa7a` `31a22f7e` `6761751e` `0ef2adaf` `fc41d773` `28dfa992` `eb06ba0d` `09a456e5` `1edbe412` `7e65d50a` `47a2cf03` `c1ea4a00` `e0f9e64f` `c94e49ff` `58df6e39` `b945b901` `e886546d` `2c803c92` `1852ced6` `596cf6d2` `397b421b` `566a19b5` `ea1e5a66` `34e893b0` `2c1551e8` `84b876a2` `b84a1304` `239cd41b` `a38fd7aa` `b9485962` `5b45600c` `528fe1e6` `02661683` `ba6d8691` `1f2e0603` `7754b241` `0604a0ec` `f7c641d9` `3adb1b94` `93672e92` `2ba47cdb` `42f0b8ac` `4a029c21` `e47f60ba` `47df29fd` `6a144421` `6aeb6106` `f580b6e3` `c21ff298` `cb9829d2` `1e09a3df` `0a44bf69` `74552947` `6976c606` `c8d059b8` `a0f9c4b3` `947c6965` `f5c9f015` `d64bde7b` `98f18288` `57e6b383` `db9d6eee`.

The frozen registry above preserves the original triage bucket names as an
immutable audit identity. It does not establish literal parity. The exhaustive
controller, configuration, UI-workflow, packaging, failure-path, restart, and
bidirectional live matrices still contain missing or unproved behavior. No
deployment-specific proof is optional under the active 1:1 goal.

## Upstream Delta Buckets

Use `scripts/collect-upstream-parity-delta.sh` to refresh the local inventory.
The generated output is intentionally a triage input, not proof of parity.

Required classification values:

- `implemented`: behavior exists and is covered by a local gate.
- `needs-proof`: behavior appears present but lacks parity-specific tests.
- `missing`: behavior is absent.
- `compat-ack`: route or workflow intentionally returns a compatibility shell.
- `not-applicable`: .NET-only implementation detail or slskdN-only packaging
  detail with no `slskR` equivalent.

## Execution Checklist

| Upstream area | slskR target | First gate |
| --- | --- | --- |
| Runtime signed capabilities | Frozen binary canonical bytes, Ed25519 sign/verify, lower-case base32 peer ID, observation-keyed bounded registry | Exact frozen unsigned bytes plus valid, forged, expired-local-metadata, malformed, blank, case-insensitive lookup, and registry-bound tests pass. |
| Capability exchange | Frozen `KSDN` custom peer-message envelope with fail-closed parsing and ack behavior | Live cross-runtime proof passes: a signed slskR hello produced an exact-nonce slskdN acknowledgement, both descriptors verified, and both live registries retained the authenticated peer observation. |
| Mesh rendezvous/overlay | BitTorrent-DHT discovery, TLS 1.3 `SLSKDNM1` framing, authenticated peer registry, JSON service calls | Live cross-runtime proof passes for slskR's pinned outbound client and slskdN's server: exact DER certificate pinning, TLS 1.3, nonce handshake, correlated frozen service framing, System.Text.Json byte arrays, `dht.Ping`, and an Ed25519-signed `dht.Store` retained by slskdN all completed. Exact rendezvous keys also announce/discover on a local Mainline testnet. |
| Private-gateway client | Pinned outbound TLS overlay plus OpenTunnel/TunnelData/GetTunnelData/CloseTunnel local TCP mapping | Live cross-runtime proof passes: slskr joined a gateway Pod hosted by slskdN, opened a policy-authorized private TCP tunnel through the pinned overlay, sent and received the exact echo bytes, and closed the tunnel. Hermetic tests retain the negative policy/member/quota/replay coverage. |
| Wishlist scheduling | Server-interval-aware scheduler with positive guardrails | Interval guard, term replacement, daemon term extraction, and scheduled search record tests are implemented. Phase C live certification received the public server interval and sent `WishlistSearch` on the authenticated connection. |
| Room failures | `CantCreateRoom`/reconnect state reaches API/UI instead of timing out silently | `CantCreateRoom` projection clears optimistic joins and records `last_error`; disconnected/reconnecting joins return HTTP `503`. |
| Multi-user private messages | Deduped, bounded recipient list and compatible API surface | Client/protocol tests cover dedupe, max count, blank recipients, and command emission; daemon API tests cover `/api/conversations/batch` recording and `MessageUsers` dispatch. |
| User groups | Sharegroup membership reaches slskd-compatible user group API | Daemon API tests cover the current `/api/users/:username/group` JSON string and the trimmed, case-insensitive, 100-user `/api/users/groups` map while preserving the default projection for unknown users. |
| Store-backed compatibility projections | Collection, wishlist, contact, share, and bridge helper routes expose local state | Daemon API tests cover `/api/shared`, `/api/contacts/nearby`, collection item update/delete/reorder, wishlist update/delete, and `/api/bridge/transfer/:id/progress` projecting existing local stores instead of fixed compatibility shells. |
| Activity and recommendation projections | Interests, now-playing, source-feed, and bridge admin compatibility routes expose local state | Daemon API tests cover user interest projection, interest-backed Soulseek recommendations, item recommendation/similar-user projections, now-playing POST/GET, source-feed preview/feed projection, and bridge admin stats deriving transfer totals instead of fixed empty compatibility shells. |
| Library, job, and discovery projections | Library health, MusicBrainz, Lidarr fallback, taste/discovery, destinations, listening-party, and multisource routes expose local state | Daemon API tests cover library health issue grouping from library items, unconfigured Lidarr wanted/missing and sync fallback from library health, Lidarr manual import seeding local library items, MusicBrainz completion/coverage, release-radar wishlist projection and persisted subscription creation, taste recommendations and discovery graph seeds, destination validation against the local destination store, joined-room listening parties with party content messages and now-playing state, generic job lists from searches/transfers, and multisource transfer job projections. |
| System mutation compatibility projections | Admin/config, application runtime, profile, bridge, relay, wishlist import, and share grant helper routes expose local state | Daemon API tests cover admin stats deriving transfer/search totals, plugin/config summaries from integration config, application restart and GC compatibility state, profile updates mutating the session projection, profile invite/cache warm/backfill/SongID/Lidarr operation counters surfacing through runtime state, bridge start/stop/config updates surfacing through bridge status and application runtime state, autoreplace/preferences state, relay/relay-agent enable-disable state, CSV wishlist import creating items, and share-grant token/backfill helpers deriving results from grant and collection stores with persisted/local status for existing grants. |
| Action-route compatibility projections | Wishlist item search, library scans/fixes, targeted library issue updates, MusicBrainz targets, and job creation/detail routes feed local stores | Daemon API tests cover wishlist item searches creating wishlist-targeted search records, library scan responses reflecting current health issues, targeted library issue updates mutating affected local item metadata, fixable library issues mutating local item state, MusicBrainz target creation projecting into library state, and discography/MusicBrainz release job creation surfacing through `/api/jobs/:id`. |
| Inventory and operations projections | Hash DB, backfill, mesh sync, logs, events, relay controller, and batch routes derive from local runtime state | Daemon API tests cover hash entries derived from share index records, backfill candidates/stats derived from search and share stores, hash backfill queue counts, mesh sync status derived from watched/capable peers, event mutation responses and logs projected from the event store, relay controller token acknowledgements carrying relay/share state, and limited local batch execution for health/config/capabilities/stats reads. |
| Media and status projections | Profile, source-feed creation, SongID, KPI, pod, federation, Solid, and security status routes expose local state | Daemon API tests cover profile data from session state, source-feed creation seeding wishlist-backed feeds, SongID runs/details/matrices deriving matches from library and share stores, KPI summaries from transfers/searches/shares, pod and federation status from room/user/mesh state, Solid status from collections/grants, and security dashboard counts from watched peers/events/webhooks. |
| Native service compatibility projections | slskdN roots, PodCore, streams, rooms, mesh health, playback, trace, fairness/ranking, port-forwarding, signal, backfill, federation, Solid, and security ban routes expose local state | Daemon API tests cover slskdN summaries from session/share/search/transfer/user/room/library stores, slskdN library health from library issues, PodCore content search from the share index, room ticker/member compatibility mutations updating joined room state, local stream availability from shares/transfers, executable peer/mesh preview ticket byte streams, security bans mutating local state and dashboard counts, fairness/ranking rows from watched users/searches/transfers, port-forwarding rows from listener bind state, federation rows from watched/capable peers, Solid rows from collections, specialized native service status routes, and the native compatibility fallback returning family-specific local counts/items/jobs instead of generic disabled shells. |
| PodCore signed membership workflow (`556fe3500`) | Implemented/hermetic | `off`, `warn`, and `enforce` signature modes match the frozen compatibility default and Ed25519 behavior. Join, join-acceptance, leave, and leave-acceptance verify the frozen canonical bytes; enforce-mode joins additionally use bounded timestamp/nonce replay protection. Join and privileged leave requests remain in a bounded in-memory pending store with versioned list/cancel routes, and room-backed membership changes occur only at the corresponding workflow transition. Hermetic tests cover valid/tampered/replayed signatures and the complete request/list/accept/leave/cancel sequence. |
| Events and live updates | Shared topic taxonomy across historical and WebSocket event feeds | Daemon API and WebSocket tests cover event recording for mutating workflows, topic projection in `/api/events/records`, topic/q/kind filtering in `/api/events`, and `/api/events/ws` frames using the same topic mapper as historical event APIs. |
| Search responses | Public and locked/private result projection | Search response tests cover flattened API ingestion, slskd-style grouped response ingestion with `files`/`lockedFiles`, protocol peer response ingestion, slskd-style `files`/`lockedFiles` split, `lockedFileCount`, per-result locked flags, `limit`/`offset` paging on search detail `results` arrays without changing total counts, paged/filtered `/api/searches/:id/responses` peer groups, caller-supplied search TTL bounds, `PUT /api/searches/:id` lifecycle/query mutation, and state-preserving cancel/fail/expire action routes. Remaining gate: required live interop against both targets. |
| Browse responses | slskd-shaped user browse and directory projection | Browse tests cover flattened API ingestion, slskd-style directory payload ingestion, peer browse payload ingestion, slskd-compatible root `directories` projection, directory `files` projection, state-backed browse status for requested/failed/cancelled/ready records, `limit`/`offset` paging without changing total directory or file counts, and `q` filtering on root directory groups and directory files. Remaining gate: required live interop against both targets. |
| Transfers | Retryable failed downloads, rooted remote-path acceptance, terminal details before failure | Transfer API and local peer tests cover explicit failed-download retry plus connected-session autonomous retry with bounded alternate-source discovery, stale reason clearing, preserved byte count for resume, scoped download paths, progress events, peer queue-position projection, stuck-download, per-user, speed, download stats, and accelerated-download compatibility reports, search-backed alternative source replacement and auto-replacement, local peer upload/download execution, and rejection paths. Remaining gate: live interop. |
| Obfuscation | Plain/obfuscated `P`, `D`, `F` fallback matrix | Loopback tests cover obfuscated `P`/`D`/`F` demux, plain rejection of obfuscated init, obfuscated rejection of plain init, preferred obfuscated file-transfer execution, and fallback to plain `P`/`F` when advertised obfuscation fails. |
| Security hardening | Bind exposure validation, feature gates, path/SSRF/logging checks | `scripts/check-remediation-baseline.sh` covers endpoint drift, browser token persistence, unsafe blank opens, WebSocket auth, CSP, webhook outbound policy, rate-limit proxy policy, storage listing pressure, transfer event growth, workflow/release policy, package matrix, dependency hygiene, release metadata, secret scanning, SDK gates, audit tooling, module hygiene, docs drift/freshness, council gates, protocol taint/adversarial checks, shell hygiene, Kubernetes public posture, and compatibility no-op documentation. |
| UI performance parity | Implemented | Rust WebUI tests assert a single active route owns live probes/workspace data, hidden RustyMilk starts stopped, diagnostics/workspaces advertise lazy state, and initial shell markup has no hidden-pane polling loops. |

## Required Gates

- `scripts/audit-parity-manifest.py --check-frozen`
- `scripts/check-endpoint-parity-drift.sh`
- `scripts/check-controller-options-differential.sh`
- `scripts/check-openapi-docs-drift.sh`
- `scripts/check-upstream-parity-classification.sh`
- `scripts/check-remediation-baseline.sh`
- `cargo test --workspace`
- Rust WebUI audit when UI behavior changes
- Live bidirectional interop matrices against both frozen daemons
- `scripts/audit-upstream-config-surface.py --require-complete`

## Assumptions

- Full feature parity is the target, including advanced slskdN features.
- Rust-native internals are acceptable when externally visible behavior matches.
- Live Soulseek and adjacent-daemon tests are mandatory for closure alongside
  the hermetic gates.
