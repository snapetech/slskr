# Bug Burndown Ledger

Date: 2026-05-05

This ledger is the canonical council queue for security and bug audit work. `docs/security-bug-burndown.md` remains the narrative findings record; this file is the operational queue used to decide ownership, acceptance criteria, remediation requirements, and release-gate coverage.

## Council Operating Model

Reviewer roles are read-only. Reviewers may add evidence, challenge confidence, and recommend regression requirements, but implementation ownership stays with the relevant owner area.

| Role | Review scope |
| --- | --- |
| Backend/Security | Rust daemon/API, auth, storage, CSP, rate limits, webhook safety, and route behavior. |
| Frontend/API Handling | React web UI, Rust web UI, browser storage, navigation sinks, event streams, and API client behavior. |
| Release/Ops | CI, release workflows, provenance, package checks, dependency policy, and Kubernetes runtime posture. |
| Client SDKs | TypeScript, Python, and Go clients, URL construction, transport auth, tests, and packaging. |
| Network Runtime | Soulseek protocol/runtime, listeners, peer transport, live interop, VPN/public posture, and transfer/search pressure. |
| Adversarial Reviewer | Abuse cases, confused-deputy paths, SSRF, secret exposure, persistence surprises, and bypass attempts. |

## Status Rules

| Status | Meaning |
| --- | --- |
| New | Candidate is plausible but not yet accepted as a real bug. |
| Accepted | Council agrees the finding is real and remediation is required. |
| In Progress | An owner is actively implementing the remediation. |
| Fixed | Code/docs have changed and the regression requirement is satisfied locally. |
| Verified | Release-gate or CI coverage proves the fix remains in place. |
| Risk Accepted | Council explicitly accepts residual risk with a dated reason. |

## Domains

Allowed domains are `Backend/API`, `Rust Web UI`, `React Web UI`, `Client SDKs`, `Release/Ops`, `Kubernetes/Deployment`, `Network Runtime`, `Tests/Tooling`, and `Docs/Config`.

## Open Council Queue

| ID | Domain | Class | Severity | Confidence | Evidence | Impact | Owner Area | Regression Requirement | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-001 | Backend/API | CSP | High | High | Generic responses now use strict or nonce-backed CSP, static web responses reject `'unsafe-inline'`, and `wasm-unsafe-eval` is scoped only to the Rust WASM shell when `slskr_web.wasm` is present. | XSS blast radius is reduced for bundled UIs while preserving documented Rust/WASM startup requirements. | Backend/Security + Rust Web UI | CSP tests reject broad inline permissions for non-WASM shells, the Rust WASM shell uses an external bootstrap module, and `scripts/check-csp-policy.sh` enforces the scoped exception. | Verified |
| BUG-002 | Release/Ops | Security scans | High | High | `scripts/run-security-scans.sh` runs Semgrep and Trivy through local binaries or pinned container images; CI and release set `SLSKR_SECURITY_SCANS_REQUIRED=1`, while local release gates remain non-blocking for missing scanner infrastructure. | Tag gates no longer silently skip security scans when tools are absent. | Release/Ops | CI/release enforce required scan mode with pinned images; local optional mode is explicit. | Verified |
| BUG-003 | Client SDKs | WebSocket auth | High | High | `client-ts/src/websocket-client.ts` now opens `/api/events/ws` with a `slskr.api-token.<percent-encoded-token>` WebSocket subprotocol, and the server parses that protocol into bearer authorization before route auth. | Browser TypeScript clients can authenticate event streams in token-auth deployments without unsupported custom WebSocket headers. | Client SDKs + Backend/Security | Authenticated browser-safe event-stream subprotocol tests in SDK/static gate and server route auth. | Verified |
| BUG-004 | React Web UI | WebSocket auth | High | High | `web/src/lib/hubFactory.js` now sends the same browser-safe auth subprotocol for session bearer tokens and omits it for passthrough/missing tokens, with Vitest coverage. | Main web event feed authenticates in token-auth deployments without leaking legacy persistent tokens or passthrough sentinels. | Frontend/API Handling + Backend/Security | Authenticated React event-feed unit coverage using the chosen browser-safe auth path. | Verified |
| BUG-005 | Backend/API | Identifier predictability | Medium | High | Webhook IDs and event IDs use UUIDv4 values in `crates/slskr/src/webhooks.rs`, and `scripts/check-webhook-outbound-policy.sh` fails if UUID generation is removed. | Predictable webhook/event IDs are no longer generated by fresh runtime records. | Backend/Security | UUID-backed ID tests and remediation gate coverage. | Verified |
| BUG-006 | Backend/API | Webhook concurrency | Medium | High | Normal webhook dispatch now receives the shared `webhook_deliveries` semaphore, uses `try_acquire_owned()`, and drops over-capacity deliveries with an explicit log. | High event volume is bounded by the same delivery pool as manual webhook test sends. | Backend/Security | Remediation gate requires the shared bounded dispatch pool; focused webhook tests pass. | Verified |
| BUG-007 | Backend/API | Outbound URL policy | Medium | High | Webhook validation now uses a central outbound policy with built-in special-use blocks plus `SLSKR_WEBHOOK_ALLOW_CIDRS` and `SLSKR_WEBHOOK_DENY_CIDRS` operator CIDR hooks. | SSRF policy is covered by shared tests for IPv4/IPv6 special-use ranges and explicit operator overrides. | Backend/Security + Adversarial Reviewer | Central outbound policy tests for IPv4/IPv6 loopback, private, link-local, documentation, multicast, and operator CIDRs. | Verified |
| BUG-008 | Backend/API | Rate limiting | Medium | Medium | Anonymous rate limiting now uses `Forwarded` or `X-Forwarded-For` only when the immediate peer matches `SLSKR_TRUSTED_PROXY_CIDRS` / `[auth].trusted_proxy_cidrs`; untrusted peers keep the raw socket address. | Reverse-proxy deployments can key anonymous clients separately without accepting spoofed forwarded headers from direct clients. | Backend/Security | Trusted proxy parsing tests with explicit allowlist and spoofing rejection cases plus `scripts/check-rate-limit-proxy-policy.sh`. | Verified |
| BUG-009 | Backend/API | Storage pressure | Medium | High | Downloads/incomplete storage directory listings now use explicit `limit`/`offset` query options, stable traversal order, lower recursive defaults, a 1,024-entry recursive max, truncation metadata, and normal HTTP rate-limit coverage. | Repeated recursive scans have a smaller per-request CPU/disk budget while preserved slskd parity routes can still page through storage roots. | Backend/Security + Network Runtime | Pagination and lower-budget recursive storage tests plus `scripts/check-storage-listing-pressure.sh` in the remediation baseline. | Verified |
| BUG-010 | Docs/Config | Compatibility persistence | Medium | High | `/api/options`, `/api/options/yaml`, and `/api/options/yaml/validate` now implement bounded target-specific overlays, exact validation, confined atomic YAML persistence, watched runtime application, and restart/reconnect signaling. | Operators and controller clients receive the frozen mutation behavior instead of a false-success acknowledgement shell. | Docs/Config + Backend/Security | Dual-target differential tests cover overlay/YAML status and payload contracts, persistence, restart/reconnect lifecycle, and malformed input; `scripts/check-compatibility-noop-documentation.sh` keeps the remaining intentional no-op inventory explicit. | Verified |
| BUG-011 | Docs/Config | Compatibility no-op inventory | Medium | High | Preserved parity routes for logs/cache/bridge/config/bans/share-grant token/backfill and MusicBrainz subscriptions are documented as empty shells or `compatibility_acknowledgement` responses, with route tests asserting the advertised shape. | Compatibility endpoints no longer look fully supported when they are intentional no-ops or empty capability shells. | Docs/Config + Backend/Security | Inventory each compatibility acknowledgement/empty shell in docs/OpenAPI and assert the advertised shape in tests. | Verified |
| BUG-012 | Release/Ops | Release SBOM | Medium | High | Release workflow now generates `slskr-cyclonedx.json` and `slskr-dependency-manifest.json` from checked-in Rust/npm/Go/Python package metadata, includes them in `SHA256SUMS.txt`, publishes them as release assets, and attests `release/*.json`. | Consumers can audit release dependencies from published assets alongside checksums and provenance attestations. | Release/Ops | `scripts/check-package-artifact-matrix.sh` requires the generator, manifest names, JSON attestation path, and verified ledger status. | Verified |
| BUG-013 | Release/Ops | Cargo package verification | Medium | Medium | `scripts/check-release-package.sh` packages the runtime crates explicitly, then runs `scripts/verify-cargo-package-contents.sh` to inspect each `.crate`, require every tracked crate input, unpack packages into a temporary workspace, restore packaged `Cargo.toml.orig` path dependencies, and run `cargo check --workspace` from the unpacked inputs. | Missing package inputs are caught while avoiding non-publishable WASM migration dependencies in the Cargo package gate. | Release/Ops | Package gate performs archive content verification plus unpacked workspace build verification, and `scripts/check-package-artifact-matrix.sh` keeps the gate registered. | Verified |
| BUG-014 | Release/Ops | Release tag policy | Medium | High | Release workflow now triggers only on `release-v*`, validates tag pushes against `release-v<semver>`, publishes only `refs/tags/release-v...`, and documents that plain `v*` or loose `release-*` tags are not release triggers. | Operators have one explicit release tag convention and malformed release tags fail before builds. | Release/Ops | `scripts/check-workflow-release-policy.sh` enforces the `release-v<semver>` docs, trigger, publish condition, and validation tokens. | Verified |
| BUG-015 | Release/Ops | Version metadata | Medium | High | Internal/unpublished Cargo crates intentionally remain `0.0.0`, `docs/release.md` documents that policy, and `scripts/check-release-version-metadata.sh` proves release artifacts derive their version from `release-v<semver>` / `SLSKR_RELEASE_VERSION` with archive roots named by version and target. | Package metadata and binary release names have an explicit, gate-checked release-version source. | Release/Ops | Release check proves artifact names, binary version, and crate metadata are intentionally aligned or documented. | Verified |
| BUG-016 | Release/Ops | Secret scanning | Medium | High | `scripts/check-secret-scanning.sh` verifies ignored local secret paths stay ignored and scans tracked files for private-key blocks and high-entropy secret-like assignments with placeholder/example allowlisting. | Accidental secret commits now fail the remediation baseline and release gate before packaging. | Release/Ops + Adversarial Reviewer | Pinned secret scanner in CI/release with placeholder allowlist coverage. | Verified |
| BUG-017 | Docs/Config | OpenAPI drift | Medium | High | Runtime `/api/openapi.json` was generated separately from checked-in `docs/openapi.json`, so API parity work could leave the served spec and documented spec divergent. Runtime OpenAPI now serves the checked-in spec through packaged crate data, with a regression test asserting equality and a drift gate requiring `docs/openapi.json` and `crates/slskr/src/openapi.json` to stay identical. | Generated clients and operators see the same OpenAPI contract whether they fetch the runtime endpoint, read docs, or install a packaged crate. | Docs/Config + Tests/Tooling | `test_runtime_openapi_matches_checked_in_spec`; `scripts/check-openapi-docs-drift.sh`; remediation baseline; package verification. | Verified |
| BUG-018 | Tests/Tooling | Compatibility smoke coverage | Medium | Medium | `.github/workflows/live-parity.yml` now runs the Rust web UI headless audit plus the hermetic local `slskd_api` automation compatibility smoke on a weekly schedule and manual dispatch, uploading screenshots, Rust web assets, and daemon logs as artifacts. It also runs the credentialed public live interop matrix when `SLSKR_LIVE_INTEROP_ENV` is configured, or uploads an explicit skipped TSV when credentials are absent. | Compatibility regressions now have a scheduled CI signal without forcing every local release gate to install external Python packages, and public live evidence is packaged whenever credentials are available. | Tests/Tooling | `scripts/check-workflow-release-policy.sh` requires the live parity workflow, schedule, Rust UI audit, slskd API smoke, credentialed live interop job, token envs, and artifact paths. | Verified |
| BUG-019 | Client SDKs | Python client gate | Medium | High | `scripts/check-python-client-quality.sh` enforces Python SDK lint/version policy, `client-python/tests/test_client.py` covers URL/path escaping, batch helpers, WebSocket URL/topic behavior, public exports, and `scripts/check-client-sdk-gates.sh` installs the dev extra, runs pytest smoke tests, and checks installed dependencies. | Python SDK regressions now fail the SDK gate instead of shipping with import-only coverage. | Client SDKs | Add lint/type/test/audit coverage, pytest smoke tests, and dependency consistency checks. | Verified |
| BUG-020 | Client SDKs | Go/Python CI coverage | Medium | High | CI/release gate install pinned Go/Python and run `scripts/check-client-sdk-gates.sh`, which runs `go test ./...` plus Python compile/import coverage. | SDK compile/runtime regressions fail CI and release gates. | Client SDKs + Release/Ops | Pinned Go/Python setup plus `go test ./...` and Python checks in CI/release. | Verified |
| BUG-021 | Release/Ops | Rust dependency hygiene | Medium | Medium | `cargo tree -d -p slskr` reports reviewed transitive duplicate roots for `base64`, `getrandom`, `hashbrown`, proc-macro `syn`, `webpki-roots`, and the temporary `rand`/`rand_core` split while upstream families converge. Direct Ed25519 users and Mainline DHT are aligned on `ed25519-dalek 3`, eliminating duplicate curve25519/Ed25519/signature roots. `docs/dev/rust-dependency-hygiene.md` records the current roots and causes, while `scripts/check-rust-dependency-hygiene.sh` fails if new duplicate roots enter the release graph without review. | The dependency review surface is bounded and future duplicate-family growth is a release-gate failure. | Release/Ops + Tests/Tooling | Duplicate-root allowlist policy plus remediation baseline gate. | Verified |
| BUG-022 | Tests/Tooling | Audit tooling availability | Medium | Medium | `scripts/check-audit-tooling.sh` requires reproducible built-in dependency inventory checks with `cargo metadata --format-version 1 --no-deps` and `cargo tree -d`, keeps `cargo audit` in documented release scans, and is registered in the remediation baseline. | Dependency inventory and duplicate-family review are reproducible without relying on locally absent optional Cargo subcommands. | Tests/Tooling | Pinned tooling or CI-native replacements such as built-in metadata/tree reports plus documented audit tooling. | Verified |
| BUG-023 | Release/Ops | GitHub Actions supply chain | Medium | High | CI/release workflow actions are pinned to reviewed 40-character commit SHAs, `docs/dev/github-actions-pin-policy.md` records the reviewed source refs and pinned commits, and `scripts/check-workflow-release-policy.sh` rejects unpinned external workflow actions or undocumented pins. | Workflow dependency trust changes now require an explicit reviewed workflow and ledger update. | Release/Ops | Actions pinned to reviewed SHAs with automated update policy. | Verified |
| BUG-024 | Network Runtime | Transfer event growth | Low | Medium | Transfer event history appends now rotates oversized `transfer-events.tsv` files at a 16 MiB byte cap, recreates the event header, preserves the prior oversized file as `transfer-events.tsv.old`, and has regression coverage in `transfer_event_append_rotates_oversized_event_file`. | Long-running nodes no longer accumulate an unbounded transfer event log. | Network Runtime | Rotation or compaction tests tied to transfer history limit or byte cap. | Verified |
| BUG-025 | Backend/API | Rust module hygiene | Low | High | Broad crate/module-level `#![allow(dead_code)]` appeared across backend modules, hiding unused compatibility surfaces from compiler review. Module-level allowances were removed from backend Rust modules, remaining compatibility allowances are item-scoped, and `scripts/check-rust-module-hygiene.sh` prevents broad suppressions from returning. | Unused compatibility code now remains visible as warnings unless a narrow item-level exception is intentionally reviewed. | Backend/Security + Tests/Tooling | `cargo check -p slskr`; Rust module hygiene gate in remediation baseline. | Verified |
| BUG-026 | Tests/Tooling | Script dependencies | Low | High | `scripts/check-dev-tooling.sh` is registered in the remediation baseline and reports required release tools plus optional live/security tooling before longer gates run. | Operators get an actionable missing-tool summary instead of late unexplained failures. | Tests/Tooling | Remediation registry and baseline require the dev-tooling preflight. | Verified |
| BUG-027 | Tests/Tooling | Test noise | Low | Medium | Unauthorized API reloads are skipped under Vitest `MODE=test`, with `web/src/lib/api.test.js` covering both test and production reload behavior. | Web test output no longer hides useful failures behind expected jsdom navigation warnings. | Frontend/API Handling | Web test run stays free of jsdom navigation warnings. | Verified |
| BUG-028 | Release/Ops | Deprecated npm transitives | Low | Medium | The unused web `eslint-config-canonical` preset was pulling deprecated lint transitive packages into the graph, and Jest's `test-exclude` selected deprecated glob 10. The preset is removed and the client package overrides the test-only glob dependency to supported glob 13. | Frontend lockfiles contain no deprecated package entries, and runtime/published dependency graphs cannot silently regain an undocumented deprecated package. | Release/Ops + Frontend/API Handling | `scripts/check-npm-dependency-hygiene.sh` rejects deprecated runtime or undocumented entries; npm advisory gates remain required. | Fixed |
| BUG-029 | Release/Ops | Dependency modernization | Low | Medium | Lockfile-resolved patch drift was found across the frontend workspaces, and the remaining major candidates are TypeScript 6 to 7 and dashboard Vitest 4 to 5. | Dependency maintenance is bounded: safe patch updates land with the current gates, while major compatibility work has an explicit tracked sequence and regression requirement. | Release/Ops + Frontend/API Handling | `docs/dev/npm-dependency-hygiene.md` defines the TypeScript/Vitest upgrade track and the required UI/client regression gates. | Fixed |
| BUG-030 | Docs/Config | Stale docs | Low | High | Stale `http_api_*` config names, `slskr:latest` image guidance, and obsolete WebSocket unsupported text were removed from general docs, and `scripts/check-docs-freshness.sh` prevents those patterns and wildcard CORS examples from returning. | Operators are less likely to copy obsolete config, mutable image tags, or outdated event-stream guidance. | Docs/Config | Grep-based docs freshness check plus doc updates or archiving. | Verified |
| BUG-031 | Tests/Tooling | Council scan loop | Medium | High | The council process could run broad scans but still promote only a small clean batch, leaving whole candidate classes unclassified. `docs/dev/council-scan-inventory.md` now tracks remaining classes, status meanings, and the active section; `scripts/run-council-scan.sh` regenerates candidate counts; `scripts/check-council-loop.sh` keeps the inventory and BUG-031 verified in the remediation baseline. | Future council cycles have a visible remaining-candidate queue instead of silently stopping after one or two fixes. | Tests/Tooling + Adversarial Reviewer | Scanner, inventory, and baseline gate require whole candidate classes to remain visible until classified. | Verified |
| BUG-032 | Client SDKs | Mutable batch inputs | Medium | High | Python SDK batch operation and response constructors accepted caller-owned dict/list objects by reference, so external mutation after construction could alter serialized batch requests or response helper results. Constructors now defensively copy mutable inputs and serialization returns a fresh copy. | Batch requests and helper responses are stable after construction and cannot be changed by caller-side mutations. | Client SDKs | Constructor mutable-input tests plus Python SDK quality gate. | Verified |
| BUG-033 | Client SDKs | Transfer chunk allocation | Medium | High | `DownloadTransfer::receive_file_from` delegated caller-supplied `remaining` directly to `FileTransferConnection::read_chunk`, which allocated a `Vec` of that size before reading. Transfer chunk reads now enforce `DEFAULT_MAX_TRANSFER_CHUNK_LEN` and fail with `FrameTooLarge` before allocation. | Malicious or mistaken transfer length metadata cannot force an oversized client-side allocation through the reusable SDK transfer helper. | Client SDKs + Network Runtime | Oversized chunk tests on `read_chunk_with_max` and `receive_file_from`; protocol count/length inventory class marked fixed. | Verified |
| BUG-034 | Backend/API | Protocol scalar narrowing | Medium | High | API routes narrowed JSON/search/message IDs into Soulseek protocol `u32` values with `as`, allowing oversized client-supplied values to wrap before protocol command emission. Search response tokens and message acknowledgement IDs now use checked `u32::try_from` and return `400` when out of range. | API inputs can no longer wrap into unrelated protocol tokens or acknowledgement IDs. | Backend/API + Network Runtime | Oversized token/ack route tests plus protocol scalar inventory classification. | Verified |
| BUG-035 | Client SDKs | Raw frame allocation | Medium | High | `read_raw_frame(reader, length)` accepted caller-supplied lengths and allocated a payload buffer without enforcing the SDK frame maximum. Raw frame reads now reject lengths above `DEFAULT_MAX_FRAME_LEN` by default and expose `read_raw_frame_with_max` for stricter callers. | SDK helpers cannot accidentally allocate arbitrary raw-frame lengths from untrusted metadata. | Client SDKs + Network Runtime | `oversized_raw_frame_is_rejected_before_payload_read`; resolver/raw-stream inventory classification. | Verified |
| BUG-036 | Client SDKs | SDK connect timeout | Medium | High | Public SDK helpers `ServerConnection::connect`, `connect_peer_messages`, `connect_distributed`, and `connect_file_transfer` delegated directly to `TcpStream::connect`, so consumers not wrapping them could hang indefinitely on slow network paths. Defaults now use `DEFAULT_CONNECT_TIMEOUT` and timeout-specific variants are available. | SDK consumers get bounded network connect behavior by default while daemon call sites can continue applying shorter configured peer timeouts. | Client SDKs + Network Runtime | `cargo test -p slskr-client`; resolver/raw-stream inventory classification. | Verified |
| BUG-037 | Client SDKs | TypeScript abort timer cleanup | Medium | High | The TypeScript SDK cleared request abort timers only after `fetch` resolved, so rejected network requests could leave timeout callbacks alive during retries. Timers are now cleared in `finally`, with a Jest regression and TypeScript build in the SDK gate. | Failed/retried TypeScript SDK requests no longer accumulate stale abort timers. | Client SDKs + Tests/Tooling | `client-ts/src/client.test.ts`; `npm test -- --runInBand`; `scripts/check-client-sdk-gates.sh` runs TS tests/build. | Verified |
| BUG-038 | Client SDKs | TypeScript zero config defaults | Medium | High | The TypeScript SDK constructor used `||` defaults, so explicit lifecycle settings such as `retries: 0`, `timeout: 0`, and `retryDelay: 0` were ignored. Defaults now use `??` so callers can disable retries or set zero-valued timing knobs deliberately. | SDK consumers can configure no-retry behavior and exact lifecycle settings without silent fallback to defaults. | Client SDKs + Tests/Tooling | `client-ts/src/client.test.ts`; `npm test -- --runInBand`; `scripts/check-client-sdk-gates.sh` runs TS tests/build. | Verified |
| BUG-039 | Docs/Config | WebSocket auth examples | Medium | High | `docs/http-api-features.md` showed raw browser `new WebSocket(...)` examples without the required token subprotocol and a Node.js example using a constructor object/header pattern that does not match browser WebSocket usage. Examples now use the supported `slskr.api-token.<encoded-token>` subprotocol array. | Operators copying raw WebSocket examples get the same auth behavior enforced by the browser/SDK WebSocket gates. | Docs/Config + Frontend/API Handling | `scripts/check-websocket-auth-coverage.sh`; example Web API inventory classification; remediation baseline. | Verified |
| BUG-040 | Network Runtime | Protocol count loop bounds | Medium | High | The first calibrated Rust protocol taint lens found unbounded wire-derived counts driving parser loops in peer search results, server string vectors/possible parents, and shared-file browse payload parsers. Counts now route through `Reader::read_bounded_count`, which rejects impossible counts based on remaining bytes and minimum bytes per item before loop execution. | Hostile count metadata can no longer force excessive parser iteration in these protocol and browse-payload paths. | Network Runtime + Backend/API | `scripts/check-rust-protocol-taint-lens.sh`; peer/server count regression tests; shared-file payload count regression tests; remediation baseline. | Verified |
| BUG-041 | Tests/Tooling | Active backlog gate | Medium | High | The Rust council all-phases runner refreshed broad candidate counts, but it did not require a durable section-level active backlog, so agents could still work one class at a time without a current visible pile list. | Future council cycles have to keep every candidate class visible with current count/status before the run can pass. | Tests/Tooling + Adversarial Reviewer | `docs/dev/bug-council-active-backlog.md`; `scripts/check-council-active-backlog.sh`; all-phases runner; remediation baseline. | Verified |
| BUG-042 | Tests/Tooling | Inventory/backlog closure drift | Medium | High | The detailed council inventory marked every candidate class fixed, but the active backlog still presented those same classes as open queues, obscuring the real remaining work. | Fixed/classified council classes now have to be reflected as guarded active-backlog rows at the fresh scan count. | Tests/Tooling + Adversarial Reviewer | `scripts/check-council-inventory-closure.sh`; active backlog; remediation baseline. | Verified |
| BUG-043 | Release/Ops | npm audit response handling | Medium | High | GitHub CI's `npm audit --json` returned a registry error object; `scripts/check-web-audit.sh` assumed `.vulnerabilities` was always an object, crashed in `jq`, and masked the actual dependency-audit failure. The runner now retries transient responses, validates the report shape, and emits the registry error when no usable report is returned. | Frontend dependency checks either produce an auditable vulnerability report or fail with the real registry/audit cause instead of an opaque parser error. | Release/Ops + Frontend/API Handling | `scripts/check-web-audit.sh` offline success and invalid-registry failure probes; GitHub CI rerun. | Fixed |
| BUG-044 | Network Runtime | Relay WebSocket backpressure | Medium | High | Relay and SignalR inbound queues and relay-agent outbound invocations were unbounded, and a stalled relay writer had no write deadline. The queues are bounded, failed challenge writes deregister protocol state, and relay frame writes now time out. | A stalled or abusive relay connection cannot grow controller memory indefinitely or leave failed handshake state behind. | Network Runtime + Backend/Security | Rust test-build, targeted relay lifecycle coverage, and GitHub CI. | Fixed |
| BUG-045 | Backend/API | Search persistence transitions | Medium | High | Search creation and eviction previously persisted expiry, identity/results, and deletion as separate database operations, allowing partial durable state when a later operation failed. Search projections and evictions now commit in one SQLite transaction and in-memory stores roll back on failure. | Search list/detail state remains consistent across persistence failures and restart instead of exposing half-written or already-evicted records. | Backend/API + Network Runtime | Persistence transaction regression coverage, route compile, and GitHub CI. | Fixed |
| BUG-046 | Network Runtime | Resumed upload progress and size validation | Medium | High | Resumed uploads reported only bytes sent during the current attempt, so a successful resumed upload could remain below 100% in the API projection. The upload path now reports the complete remote offset plus newly sent bytes and rejects a local share whose size differs from the negotiated size. | Transfer state and completion metrics no longer under-report successful resumed uploads or claim success for a truncated local source. | Network Runtime + Backend/API | Resumed upload integration fixture asserts the peer receives the suffix and the transfer projection reaches the negotiated size. | Fixed |
| BUG-047 | Network Runtime | Compatibility obfuscated fallback | Medium | High | Compatibility mode advertised regular-first with obfuscated fallback, but the dial-order function omitted the obfuscated candidate whenever the regular endpoint was absent. A peer advertising only a usable type-1 endpoint could therefore not be contacted. | Regular-first compatibility preserves connectivity to peers that expose only an obfuscated endpoint. | Network Runtime | Dial-order regression covers compatibility mode with no regular endpoint and a valid type-1 endpoint. | Fixed |
| BUG-048 | Release/Ops | npm audit registry outage handling | Medium | High | GitHub CI stopped before all frontend gates when the npm audit endpoint returned no usable report after retries, even though the installed lockfile graph and cached advisory metadata were available. All three frontend graphs now use the validated retry/offline audit runner. | A transient registry audit outage no longer blocks unrelated CI work or hides real dependency advisories in web, dashboard, or client-ts. | Release/Ops + Frontend | Offline cached-report success and invalid-registry fallback path for all three frontend packages; GitHub CI rerun. | Fixed |
| BUG-049 | Backend/API | Transfer completion invariants | Medium | High | Transfer progress and completion routes accepted missing or oversized byte counts, arbitrary non-terminal statuses, successful partial completions, and emitted a completion webhook for cancellation. The routes now validate mutations before changing the projection and suppress webhooks for cancellation. | API consumers cannot manufacture impossible transfer states, report a partial transfer as successful, or receive a false completion notification. | Backend/API | Controller transfer API regression covers invalid status, oversized progress, partial-success rejection, and required progress input. | Fixed |
| BUG-050 | Frontend/Security | Unsafe external URL handling | Medium | High | Blank-tab helpers passed server-derived URLs directly to `window.open`, allowing unsafe `javascript:` or `data:` schemes if a remote response was compromised. The helper now permits only HTTP(S) URLs and relative URLs resolved to HTTP(S), and privilege usernames are query-encoded. | Stream and external-link actions cannot execute script URLs through the blank-tab helper. | Frontend/Security | Vitest safe-open regression for script/data schemes and encoded privilege URL. | Fixed |
| BUG-051 | Frontend/Runtime | Search and monitoring lifecycle races | Medium | High | Completed-search result rows could lag the terminal projection, unknown search routes navigated during render, and monitoring refreshes could update state after unmount or let stale requests win. Bounded result retries, effect-owned navigation, and abort/active guards now close those lifecycle races. | Search detail pages recover rows that persist shortly after completion, invalid routes do not render blank content, and monitoring state is not updated by stale requests. | Frontend | Search detail lifecycle regression, React route rendering, and dashboard type/test gates. | Fixed |
| BUG-052 | React Web UI | Compatibility workspace dead controls and async races | Medium | High | Native messaging rendered a static shell with dead controls, compatibility browse exposed a no-op Browse button, and Users, Events, and BrowseSession could leak listeners or apply stale asynchronous responses after navigation or unmount. Both runtime profiles now use live workspace/session paths with owned cleanup and response guards. | Native and compatibility users can browse, message, and inspect event history through live controls without stale responses freezing or overwriting the current view. | Frontend/API Handling | Native messaging regression, malformed/error event rendering, and Web UI lint/build/test gates. | Fixed |
| BUG-053 | React Web UI | Event pagination contract | Medium | High | The React Events view requested paged `/api/v0/events` results and read `X-Total-Count`, but the daemon emitted no total-count header, so the view always rendered zero pagination pages. The controller now emits the filtered total while preserving the top-level array contract. | Users can page through event history and retain accurate pagination when kind, topic, or text filters are active. | Backend/API + Frontend/API Handling | Event-store filtered-count coverage, live HTTP header coverage, release-note fragment, and Web UI event pagination gate. | Fixed |
| BUG-054 | React Web UI | Event filter parity | Medium | High | The API exposed `kind`, `topic`, and text query filters, but the React Events page only sent pagination parameters and gave operators no way to use the server-side filters. The page now applies all three filters through encoded client requests and resets pagination when filters change. | Operators can narrow event history without downloading unrelated records or losing the filtered total. | Frontend/API Handling | Events client URL-encoding coverage, Events UI filter regression, release-note fragment, and Web UI test/build gates. | Fixed |
| BUG-055 | Client SDKs | Event filter parity | Medium | High | TypeScript, Python, and Go SDK event helpers exposed only pagination and optional kind filtering even though the daemon contract also supports topic and text filters. All three SDKs now expose those filters while preserving existing method signatures. | SDK consumers can request the same filtered event history as the HTTP API and web UI without constructing raw URLs. | Client SDKs | TypeScript URL/query coverage, Python request-parameter coverage, Go compatibility/new-method coverage, release-note fragment, and SDK gates. | Fixed |
| BUG-056 | Client SDKs | Python WebSocket listener lifecycle | Medium | High | Python WebSocket unsubscribe closures indexed the listener map directly after `remove_all_listeners()`, raising `KeyError`, and callbacks that removed themselves during dispatch could invalidate the active set iterator. Unsubscribe now tolerates cleared registries and dispatch iterates a snapshot. | Python event-feed consumers can tear down listeners during shutdown or callback execution without losing delivery or raising a cleanup exception. | Client SDKs | Python listener mutation/cleanup regression and full SDK gate. | Fixed |
| BUG-057 | Client SDKs | Go event response shape | Medium | High | Go `GetEvents` used the object-only response helper even though `/api/events` returns a top-level array in the documented daemon contract; the existing fixture only exercised an optional envelope. The SDK now accepts the top-level array and still retains envelope compatibility. | Go SDK consumers can read event history from the daemon’s primary response shape without a response-format error. | Client SDKs | Go top-level event-array fixture plus full SDK gate. | Fixed |
| BUG-058 | Client SDKs | TypeScript WebSocket reconnect lifecycle | Medium | High | TypeScript WebSocket disconnect left the closing socket installed until the browser delivered its asynchronous close event, so an immediate reconnect was rejected as already in progress. Disconnect now invalidates the socket synchronously and stale callbacks cannot clear a replacement connection. | TypeScript SDK consumers can reconnect immediately after an intentional disconnect without waiting for browser event-loop cleanup. | Client SDKs | TypeScript deferred-close reconnect regression and SDK gates. | Fixed |
| BUG-059 | Client SDKs | SDK listener failure and closed-channel handling | Medium | High | Python connection/error callbacks were launched as background tasks without consuming task failures, while Go channel listeners could panic the WebSocket reader when callers closed a registered channel. Python now consumes and routes callback failures, and Go delivery isolates closed channels. | SDK event feeds remain alive when application listeners fail or are torn down during connection and error notification. | Client SDKs | Python callback-task regression, Go closed-channel regression, and SDK gates. | Fixed |
| BUG-060 | Network Runtime | Mesh-sync confined chunk reads | High | High | Mesh-sync `ReqChunk` handling opened indexed share paths with path-based `File::open`, so a swapped parent directory could bypass the configured share-root confinement and read an unrelated file. Chunk reads now use the shared confined/no-follow descriptor opener and reject files whose size differs from the indexed entry. | Mesh-sync peers cannot use a stale or swapped shared path to read outside configured share roots, and changed shared files fail closed instead of returning mismatched chunks. | Network Runtime + Backend/API | `mesh_sync_chunk_reads_remain_confined_to_share_roots`; mesh chunk size regression; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-061 | Release/Ops | Main CI cancellation | High | High | The CI concurrency policy canceled in-progress and queued runs for `main` whenever another push arrived, so frequent fix pushes left the release-required commit without a completed result and made canceled runs look like recurring failures. Main pushes now use commit-specific concurrency groups and no cancellation, while pull requests retain ref-scoped superseded-run cancellation. | Every pushed `main` commit has a durable CI result that the release workflow can consume; pull-request feedback remains bounded to the newest revision. | Release/Ops | `scripts/check-workflow-release-policy.sh`; GitHub CI runs for multiple pushed commits; release-note validation. | Fixed |
| BUG-062 | Network Runtime | Mesh-sync invalid-message limit | High | High | `MeshSyncSecuritySettings.max_invalid_messages_per_window` was loaded, validated, and projected but never consumed; runtime accounting only enforced the invalid-entry limit. Mesh security state now tracks bounded per-peer invalid-message and invalid-entry counts independently and applies both thresholds before allowing the window to continue. | Operators’ configured mesh message-rate protections now take effect, limiting peers that send too many invalid frames even when each frame contains few invalid entries. | Network Runtime + Backend/Security | `sync_violation_state_enforces_message_and_entry_limits_separately`; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-063 | Network Runtime | Event WebSocket initial heartbeat race | Medium | High | The event WebSocket used Tokio’s immediately-ready first interval tick, so a control-frame close and the first server heartbeat could race and emit `PING` before the required close acknowledgement. The heartbeat now starts after one full configured interval. | Newly connected event clients can complete close/control handshakes deterministically before liveness traffic begins. | Network Runtime + Backend/API | `websocket_close_frame_is_acknowledged_before_shutdown`; focused event WebSocket tests; release-note validation. | Fixed |
| BUG-064 | Network Runtime | Browse wire-record amplification | High | High | Peer browse parsers bounded individual counts by remaining bytes but allowed a compressed response to contain millions of empty sections, folders, or non-file records under the decompression cap, consuming CPU without increasing the bounded result set. All sibling browse parsers now cap total wire sections, folders, and file records before iteration. | Malformed peer browse responses cannot spend an unbounded amount of parser work on discarded metadata or empty structures. | Network Runtime + Backend/API | `MAX_BROWSE_WIRE_*` caps; `shared_file_list_payload_rejects_excessive_wire_records_without_entries`; `shared_file_list_payload_rejects_excessive_sections_without_files`; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-065 | Backend/API | Bloom preview allocation pressure | High | High | The MusicBrainz Bloom preview clamped expected item counts but accepted arbitrarily small false-positive rates, allowing an authenticated request to make the filter formula allocate hundreds of megabytes before producing a response. Bloom construction now rejects requests above a fixed byte budget before allocation. | Extreme Bloom precision requests fail with a bounded 400 response instead of consuming excessive daemon memory. | Backend/Security + Backend/API | `MAX_BLOOM_FILTER_BYTES`; `filter_rejects_unbounded_precision_requests`; library-bloom route regression; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-066 | Backend/API | Versioned multisource limit bypass | High | High | The native `/api/v0/multisource/swarm*` compatibility route parsed file and chunk sizes independently of the executable multisource validator, accepting oversized files or chunks and retaining more discovered source names than the executor permits. The route now shares the size/chunk validator and caps discovered sources at the executor ceiling. | Versioned swarm requests fail closed at the same resource limits as direct multisource execution and cannot create oversized compatibility jobs. | Backend/Security + Backend/API | `validate_file_size_and_chunk_size`; `versioned_swarm_rejects_oversized_transfer_limits_before_discovery`; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-067 | Backend/API | Merge-array pre-deserialization pressure | High | High | HashDb, shadow-index, realm-index, and legacy mesh merge handlers delegated caller-controlled JSON arrays to `serde_json::from_value` before enforcing the lower store batch limits. The dispatchers now reject oversized arrays from the parsed JSON envelope before cloning and deserializing them into owned records, with the native and legacy limits kept aligned. | Oversized sync batches fail at the route boundary instead of allocating a full typed batch that the store will reject afterward. | Backend/Security + Backend/API | `json_array_exceeds_limit`; `merge_routes_reject_oversized_arrays_before_store_deserialization`; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-068 | Backend/API | Unexecutable versioned swarm queue | High | High | The native versioned swarm route accepted requests without an expected SHA-256, discovered only peer usernames, inserted a queued job with no output path, and never invoked the executor. Versioned swarm requests now require `expectedHash` before queue insertion and route source discovery through the real verified executor. | Clients cannot receive a successful background-job response for a swarm that has no verifiable execution path; invalid requests fail before creating orphan jobs. | Backend/API + Network Runtime | `versioned_swarm_requires_expected_hash_before_queueing`; versioned multisource execution regression; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-069 | Backend/API | Multisource source-array pressure | High | High | Native versioned swarm requests deserialized every caller-supplied source object before applying the executor’s 16-source limit, while route selection cloned the source array a second time. Explicit source arrays are now bounded before typed deserialization, route selection borrows the parsed array, and mesh discovery truncates to the same executor ceiling. | Oversized source batches fail at the API boundary with bounded allocation and CPU work, while automatically discovered source lists cannot exceed the executor’s supported limit. | Backend/Security + Backend/API | `versioned_swarm_rejects_oversized_source_batches_before_deserialization`; `sources.truncate(multisource::MAX_SOURCES)`; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-070 | Backend/API | Versioned download executor gap | High | High | The native versioned `/api/v0/multisource/download` route counted legacy source usernames and returned a successful HTTP response containing an “unavailable” result even when callers supplied the real HTTP range-source shape supported by the swarm executor. Range-source requests now enter the verified executor, persist the real job/result state, and retain the old username/fullPath projection only for compatibility payloads. | Clients using the versioned download route with verifiable HTTP sources receive actual bounded swarm execution and failure/completion results instead of a fabricated compatibility stub. | Backend/API + Network Runtime | `versioned_download_range_sources_use_verified_executor`; `multisource_versioned_download_response`; runtime-boundary hardening gate; release-note validation. | Fixed |

| BUG-071 | Backend/API | Browse response wire amplification | High | High | The JSON browse-response route iterated every caller-supplied top-level entry and every nested directory file before the bounded browse store could discard excess records. The native and legacy dispatchers now reject oversized directory and aggregate file arrays at the parsed JSON boundary. | Malformed browse-response requests cannot force unbounded entry conversion and temporary allocation before the persistent browse limits apply. | Backend/Security + Backend/API | `browse_response_exceeds_wire_limits`; `browse_response_rejects_oversized_wire_batches_before_store_mutation`; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-072 | Release/Ops | All-features differential test harness | High | High | Bounded differential async tests were compiled with `#[tokio::test]` wrappers even when the explicit bounded runners were enabled, so the runners attempted to await synchronous test wrappers and the all-features CI target failed with hundreds of `E0277` errors. Bounded-feature builds now keep those functions as futures for the runner, while full-only tests retain individual registration. | The broad Rust lint/build target compiles the complete differential harness instead of failing before tests run. | Release/Ops + Backend/API | `cargo clippy -p slskr --all-features --all-targets -- -D warnings`; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-073 | Backend/API | Search response wire amplification | High | High | The JSON search-response route converted every caller-supplied `files` and `lockedFiles` element before the bounded search store could discard excess results. The native and legacy dispatchers now reject aggregate arrays above the per-search result ceiling at the parsed JSON boundary. | Malformed search-response requests cannot force unbounded result conversion and temporary allocation before the persistent search limits apply. | Backend/Security + Backend/API | `search_response_exceeds_wire_limits`; `search_response_rejects_oversized_wire_batches_before_store_mutation`; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-074 | Backend/API | Collection and MusicBrainz batch wire amplification | High | High | Collection reorder, MusicBrainz Bloom diff, Bloom wishlist, and unversioned discography-wishlist routes cloned or converted caller-supplied arrays before applying their existing bounded collection, library, or wishlist ceilings. The routes now reject oversized arrays at the parsed JSON boundary, and the collection store keeps the same guard for internal callers. | Oversized controller batches cannot force temporary array conversion or quadratic reorder work before bounded state limits apply. | Backend/Security + Backend/API | `collection_reorder_exceeds_wire_limits`; `collection_reorder_rejects_oversized_wire_batches_before_store_mutation`; `musicbrainz_rejects_oversized_json_batches_before_state_mutation`; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-075 | Backend/API | Transfer request wire amplification | High | High | Generic, per-user, and batch download routes accepted arbitrary `files` arrays and converted them before transfer admission and active-slot limits ran. All transfer ingress paths now reject arrays above the shared 10,000-file request ceiling before queue construction or transfer state mutation. | Oversized download requests fail with a bounded 400 response instead of creating large temporary filename, path, and transfer-entry work sets. | Backend/Security + Backend/API | `MAX_TRANSFER_REQUEST_FILES`; `controller_file_array_exceeds_wire_limits`; `transfer_rejects_oversized_file_batches_before_queue_mutation`; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-076 | Backend/API | String-array wire amplification | High | High | Conversation batch, wishlist bulk-filter, and capability negotiation endpoints cloned caller-supplied JSON string arrays before applying their recipient, wishlist-item, or capability limits. The dispatch boundaries now reject arrays above the endpoint-specific ceilings before string conversion, state lookup, or response construction; the versioned wishlist validator also recognizes `bulk-filter` as a static endpoint instead of treating it as an invalid item ID. | Oversized string-array requests receive bounded 400 responses without allocating or iterating work sets larger than the endpoint contracts allow, and the versioned bulk-filter route reaches its intended handler. | Backend/Security + Backend/API | `MAX_CAPABILITY_NEGOTIATION_ITEMS`; `json_array_field_exceeds_limit`; `conversation_batch_exceeds_wire_limits`; `wishlist_bulk_filter_exceeds_wire_limits`; `string_array_routes_reject_oversized_wire_batches_before_mutation`; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-077 | Backend/API | MediaCore batch wire amplification | High | High | MediaCore publish, republish, retrieve, and cache-clear routes cloned caller-supplied descriptor, ContentID, or cache-key arrays before bounded work admission; publish and retrieve batches had no route-level ceiling. Each ingress path now rejects more than 100 items before cloning, state lookup, or per-item work. | Oversized MediaCore requests fail with bounded 400 responses instead of allocating unbounded descriptor/result work or scanning cache state for excessive keys. | Backend/Security + Backend/API | `MAX_MEDIACORE_BATCH_ITEMS`; `mediacore_rejects_oversized_wire_batches_before_work`; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-078 | Backend/API | Remaining controller array wire amplification | High | High | Incoming share, extended download, multisource compatibility, quarantine-jury, ranking, MediaCore IPLD/portability, and listening-party routes either cloned caller arrays before their existing truncation or had no batch ceiling. The shared route boundaries now reject oversized work sets or borrow only the bounded prefix before conversion, while native and legacy share-announcement paths stay aligned. | Oversized controller payloads fail before queue, state, routing, or per-item validation work can amplify caller-controlled arrays. Normal bounded payloads retain their existing response and truncation behavior. | Backend/Security + Backend/API | `MAX_INCOMING_SHARE_ITEMS`; `MAX_EXTENDED_DOWNLOAD_ITEMS`; `MAX_QUARANTINE_JURY_ITEMS`; `MAX_RANKING_BATCH_ITEMS`; `remaining_controller_array_routes_reject_oversized_wire_batches_before_work`; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-079 | Backend/API | Controller routing and remediation array pressure | High | High | Library remediation, release-radar subscription/route, PodCore peer-routing, and PodCore discovery registration handlers iterated or copied caller arrays before their small operational fan-out limits, allowing oversized requests to consume conversion, deduplication, or routing work. The handlers now reject arrays above their endpoint-specific ceilings before those operations. | Oversized remediation, routing, subscription, and discovery requests fail before state mutation or peer fan-out, while valid route and registration payloads retain their existing behavior. | Backend/Security + Backend/API | `MAX_ROUTING_TARGET_PEERS`; `MAX_RADAR_MUTED_RELEASE_GROUPS`; `MAX_POD_DISCOVERY_TAGS`; `remaining_controller_array_routes_reject_oversized_wire_batches_before_work`; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-080 | Backend/API | Legacy dispatcher merge-array parity | High | High | The opt-in legacy controller dispatcher deserialized hash-sync, shadow-index, and embedded realm-index arrays before applying the bounded route-group limits, leaving compatibility verification on a larger allocation path than production dispatch. The legacy branches now validate the same endpoint-specific array ceilings before typed deserialization. | Legacy and route-group dispatchers reject the same oversized merge payloads before store mutation or typed batch allocation. | Backend/Security + Backend/API | `json_array_exceeds_limit`; `merge_routes_reject_oversized_arrays_before_store_deserialization` with `legacy-route-dispatch`; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-081 | Release/Ops | Legacy dispatcher stack safety | Medium | High | The opt-in monolithic legacy dispatcher could overflow the default Tokio worker stack while polling merge routes with bounded-but-large JSON batches, so its array parity guard was not reliably observable. Legacy hash-sync and shadow-index merge requests now enter the split dispatcher, preserving the same contract with bounded stack usage. | Diagnostic legacy-mode merge requests complete with the production bounded response instead of aborting the worker with a stack overflow. | Release/Ops + Backend/Security | `legacy merge paths use bounded route dispatcher`; legacy merge-array regression; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-082 | Backend/API | CSV and source-preview row amplification | High | High | Versioned and unversioned wishlist CSV imports, local CSV previews, and the unversioned source-preview formatter materialized every parsed row before applying the store or response limits. The shared CSV and source-preview parsers now reject more than 10,000 non-empty rows before building typed import or response work, and both dispatcher profiles use the same bounded paths. | Oversized CSV and local source-preview payloads fail with a bounded 400 response before wishlist mutation, history mutation, or preview-row allocation; valid imports and previews retain their existing row shapes. | Backend/Security + Backend/API | `MAX_CSV_IMPORT_ROWS`; `MAX_SOURCE_PREVIEW_ROWS`; `parse_simple_wishlist_import_rows`; `parse_simple_source_preview_items`; `wishlist_csv_import_rejects_oversized_row_batches`; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-083 | Backend/API | Realm subject-index nested collection amplification | High | High | Realm subject-index synchronization bounded only the outer `realmIndexes` array. A valid-sized index could still carry unbounded entries, aliases, evidence links, external-id maps, or WorkRef metadata, forcing validation, payload hashing, conflict indexing, and persistence work before the 8 MiB state guard. Index validation now rejects oversized nested collections before hashing or entry traversal, while preserving the existing signed-index contract for bounded payloads. | Oversized nested realm-index payloads fail with a bounded 400 response before index state mutation; bounded signed indexes retain their existing merge and conflict behavior. | Backend/Security + Backend/API | `MAX_ENTRIES_PER_INDEX`; `MAX_ALIASES_PER_ENTRY`; `MAX_EVIDENCE_LINKS_PER_ENTRY`; `MAX_EXTERNAL_IDS_PER_ENTRY`; `MAX_WORK_REF_METADATA_FIELDS`; `merge_rejects_oversized_nested_index_collections_before_hashing`; runtime-boundary hardening gate; release-note validation. | Fixed |
| BUG-084 | Client/Protocol | Server list object-count amplification | High | High | Several server-protocol list decoders relied only on the remaining frame-byte calculation. Maximum-sized frames could therefore reserve and populate hundreds of thousands or millions of recommendation, similar-user, room-ticker, possible-parent, or joined-room user objects. The shared server-list ceiling now applies before vector allocation and is mirrored by encoders. | Oversized server list counts fail during protocol decoding without allocating the declared object batch; bounded list messages retain their existing wire shape. | Backend/Security + Client SDKs | `MAX_SERVER_LIST_ITEMS`; `server_list_decoders_reject_object_count_amplification`; protocol test suite. | Fixed |
| BUG-085 | Backend/API | Shadow-index merge partial mutation | High | High | The combined shadow-index synchronization route committed valid shadow records before validating its accompanying realm indexes, so a rejected realm index returned `400` after mutating shadow state. Realm-index batches are now validated in full before the route touches shadow records, and the realm store itself validates a complete batch before insertion. | Rejected combined synchronization requests leave both shadow and realm-index state unchanged. | Backend/Security + Backend/API | `validate_indexes`; `merge_validates_the_entire_batch_before_mutation`; `merge_routes_reject_oversized_arrays_before_store_deserialization` partial-write assertion; release-note validation. | Fixed |
| BUG-086 | Client SDKs | Blank inbound search query | Medium | Medium | Client search responders treated a whitespace-only query as syntactically valid and delegated it to the share index. The responder now rejects empty normalized term sets before applying custom index logic. | A blank peer search cannot accidentally become a broad search through a custom share-index implementation. | Client SDKs + Network Runtime | `responder_rejects_blank_queries_before_searching_the_index`; client search tests; release-note validation. | Fixed |

## Council Gate Mapping

| Gate | Purpose |
| --- | --- |
| `scripts/check-endpoint-parity-drift.sh` | Preserve current parity endpoint coverage and fail if the canonical endpoint list drifts from the router. |
| `scripts/check-browser-token-persistence.sh` | Prevent browser API tokens and ListenBrainz tokens from returning to persistent `localStorage`. |
| `scripts/check-unsafe-blank-opens.sh` | Prevent reverse-tabnabbing regressions in browser links and `window.open` calls. |
| `scripts/check-websocket-auth-coverage.sh` | Enforce browser WebSocket auth subprotocol coverage across server, TypeScript SDK, and React event feed. |
| `scripts/check-csp-policy.sh` | Prevent broad inline CSP allowances and keep the WASM execution exception scoped to the Rust WASM shell. |
| `scripts/check-webhook-outbound-policy.sh` | Keep webhook registration validation, UUID webhook/event IDs, and bounded outbound delivery behavior enforced. |
| `scripts/check-rate-limit-proxy-policy.sh` | Ensure forwarded client IP rate-limit keys are used only behind explicitly trusted proxy CIDRs. |
| `scripts/check-storage-listing-pressure.sh` | Keep recursive storage listings on a lower traversal budget with pagination metadata and regression coverage. |
| `scripts/check-transfer-event-growth.sh` | Keep transfer event logs bounded by a byte-cap rotation path with regression coverage. |
| `scripts/check-workflow-release-policy.sh` | Enforce current release workflow hardening and keep accepted release-policy findings registered. |
| `scripts/run-security-scans.sh` | Run Semgrep and Trivy in required CI/release mode or explicit non-blocking local mode. |
| `scripts/check-package-artifact-matrix.sh` | Verify release archive targets and keep SBOM/version/package gaps registered. |
| `scripts/check-release-version-metadata.sh` | Keep the internal crate-version policy documented and prove release artifacts derive version metadata from the release input. |
| `scripts/check-secret-scanning.sh` | Scan tracked files for committed secret patterns and verify local secret files remain ignored. |
| `scripts/check-python-client-quality.sh` | Enforce Python SDK lint/version policy before the heavier SDK gate runs pytest smoke checks. |
| `scripts/check-client-sdk-gates.sh` | Run Go SDK tests and Python SDK lint, compile, pytest, import, and dependency checks in CI and release gates. |
| `scripts/check-audit-tooling.sh` | Run reproducible Cargo metadata and duplicate-dependency inventory checks and keep audit tooling registered. |
| `scripts/check-npm-dependency-hygiene.sh` | Reject deprecated runtime or undocumented frontend lockfile dependencies and keep direct lint imports declared. |
| `scripts/check-dev-tooling.sh` | Fail fast when required local release tools are absent and summarize optional live/security tools. |
| `scripts/check-openapi-docs-drift.sh` | Validate checked-in OpenAPI JSON and keep OpenAPI/compatibility documentation gaps registered. |
| `scripts/check-docs-freshness.sh` | Block stale config/image/WebSocket/CORS guidance from returning to general docs. |
| `scripts/run-council-scan.sh` | Regenerate broad candidate counts for the council inventory. |
| `scripts/run-council-bughunt.sh` | Run the actual council bughunt entrypoint: fresh counts, process gates, semantic lenses, and pending phase visibility. |
| `scripts/check-council-active-backlog.sh` | Ensure every current council scan class has a durable count/status row before a council pass can be treated as resumable. |
| `scripts/check-council-inventory-closure.sh` | Ensure fixed inventory classes are represented as guarded rows at the current active-backlog count. |
| `scripts/check-council-loop.sh` | Keep the council scan inventory, status model, and remaining candidate classes visible in the baseline. |
| `scripts/check-rust-protocol-taint-lens.sh` | Calibrated Rust protocol taint lens for reader-derived counts flowing into allocations, reads, or loops. |
| `scripts/check-rust-protocol-adversarial-corpus.sh` | Multi-seed and known-corpus adversarial protocol decoder regression gate. |
| `scripts/check-shell-script-hygiene.sh` | Run shell syntax checks and flag common script footguns. |
| `scripts/check-kubernetes-public-posture.sh` | Enforce single-replica, PVC-backed, authenticated-metrics, restricted Kubernetes defaults. |
| `scripts/check-compatibility-noop-documentation.sh` | Keep preserved compatibility acknowledgements documented and tracked until route docs/tests fully cover them. |
| `scripts/check-remediation-script-registry.sh` | Ensure every `scripts/check-*.sh` gate is executable and registered in the baseline. |
