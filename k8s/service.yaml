apiVersion: v1
kind: Service
metadata:
  name: slskr-api
  namespace: slskr
  labels:
    app: slskr-api
spec:
  type: ClusterIP
  selector:
    app: slskr-api
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
