---
category: changed
audience: users, operators
area: web-ui
action: none
breaking: false
---

When a web content root contains `slskd` and `slskdn` profile builds, the
controller now serves the build selected by the compatibility target, including
SPA navigation to `/dashboard`.
