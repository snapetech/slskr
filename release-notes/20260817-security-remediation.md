---
category: security
audience: users, operators
area: security-remediation
action: Configure explicit controller and metrics credentials when authentication is enabled.
breaking: true
---

Security updates remove the legacy built-in controller and metrics password, refresh vulnerable Rust and JavaScript dependencies, and restrict release workflows to explicitly named secrets. Configure credentials explicitly before enabling authenticated access.
