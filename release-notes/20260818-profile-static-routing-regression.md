---
category: fixed
audience: users, operators
area: web-ui
action: none
breaking: false
---

Compatibility-profile static delivery now prefers a selected `slskd` or
`slskdN` build across all configured search roots, while health endpoints remain
JSON responses instead of being captured by the SPA fallback.
