---
category: fixed
audience: operators
area: live-validation
action: none
breaking: false
---

Operators can run the VPN-scoped HTTP transfer smoke with namespace-safe Soulseek listener, DHT, and overlay ports, current browse responses, absolute share paths, and a verified prebuilt binary. It proves authenticated browse, queued transfer bytes, hash integrity, and session soak without competing Cargo jobs.
