---
category: changed
audience: users
area: web-ui
action: none
breaking: false
---
The web UI now uses a single-accent "Oxide" color identity (distinct from slskdN's "Iris") plus four shared semantic colors used only for real state; the player collapses by default until explicitly expanded; top navigation groups low-traffic destinations under Discover/Network/Sharing menus; and System settings groups its 22 tabs into six named sections instead of one flat strip.
