---
category: fixed
audience: operators
area: container-publishing
action: none
breaking: false
---

The container shutdown smoke test now uses a search utility available in the minimal runtime image, so release validation no longer depends on development-only tooling inside the image.
