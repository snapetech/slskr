---
category: fixed
audience: users, operators
area: network-dashboard
action: none
breaking: false
---
The Network dashboard now displays mesh and discovered peers returned by compatibility endpoints, including the standard `{ peers, count }` response envelope, instead of incorrectly showing an empty peer state.
